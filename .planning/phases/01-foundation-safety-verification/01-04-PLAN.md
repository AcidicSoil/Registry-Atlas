---
phase: 01-foundation-safety-verification
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
  - README.md
  - public/index.legacy.html
  - public/vite.svg
  - src/main.ts
  - src/counter.ts
  - src/style.css
  - src/typescript.svg
autonomous: true
requirements:
  - FOUND-03
must_haves:
  truths:
    - "User only reaches the canonical Registry Atlas app surface per FOUND-03, D-05, D-06, and D-07."
    - "Legacy and Vite starter deployable artifacts are removed when unused."
    - "Generated `dist/` remains untracked and its policy is explicit per D-08."
  artifacts:
    - path: "index.html"
      provides: "Canonical static shell and document title"
      contains: "<title>Registry Atlas</title>"
    - path: "README.md"
      provides: "Canonical app surface and generated output policy"
      contains: "index.html"
    - path: "public/index.legacy.html"
      provides: "Removed deployable legacy page"
      exists: false
  key_links:
    - from: "index.html"
      to: "src/registry-explorer/entry.ts"
      via: "module script"
      pattern: "src/registry-explorer/entry.ts"
    - from: "README.md"
      to: "public/styles/registry-explorer.css"
      via: "canonical surface docs"
      pattern: "public/styles/registry-explorer.css"
---

## Phase Goal

**As a** Registry Atlas maintainer, **I want to** safely evolve the existing app with runnable tests, canonical deployed surface, safe rendering primitives, and one vocabulary source of truth, **so that** later official-directory import work can proceed without silent verification, deployment, rendering, or taxonomy drift.

<objective>
Remove stale deployed and starter surfaces so the canonical Registry Atlas app is the only user-reachable app surface. This implements FOUND-03 and honors D-05, D-06, D-07, and D-08.

Purpose: Safety fixes in TypeScript renderers and tests do not protect a stale public legacy page or confusing starter entry files, so the deployed surface must be canonical before later phases.
Output: Removed legacy/starter artifacts, canonical document title, README canonical surface guidance, and explicit generated-output policy.
</objective>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-safety-verification/01-CONTEXT.md
@.planning/phases/01-foundation-safety-verification/01-RESEARCH.md
@.planning/phases/01-foundation-safety-verification/01-PATTERNS.md
@.planning/phases/01-foundation-safety-verification/01-UI-SPEC.md
@.planning/codebase/CONCERNS.md
@index.html
@README.md
@.gitignore
@public/index.legacy.html
@src/main.ts
@src/registry-explorer/entry.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy and starter files from deployable/source surface</name>
  <read_first>
    <file>public/index.legacy.html</file>
    <file>public/vite.svg</file>
    <file>src/main.ts</file>
    <file>src/counter.ts</file>
    <file>src/style.css</file>
    <file>src/typescript.svg</file>
    <file>index.html</file>
    <file>.planning/phases/01-foundation-safety-verification/01-CONTEXT.md</file>
    <file>.planning/phases/01-foundation-safety-verification/01-PATTERNS.md</file>
  </read_first>
  <files>public/index.legacy.html, public/vite.svg, src/main.ts, src/counter.ts, src/style.css, src/typescript.svg</files>
  <action>Delete `public/index.legacy.html` rather than archiving it unless current repo evidence shows a concrete historical requirement. Delete unused Vite starter artifacts `public/vite.svg`, `src/main.ts`, `src/counter.ts`, `src/style.css`, and `src/typescript.svg` when `rg` confirms they are not referenced by `index.html`, `src/registry-explorer/**`, README, or docs. Do not touch `public/styles/registry-explorer.css`; it is part of the canonical app surface. Do not add `dist/` to source control.</action>
  <verify>
    <automated>test ! -e public/index.legacy.html && test ! -e public/vite.svg && test ! -e src/main.ts && test ! -e src/counter.ts && test ! -e src/style.css && test ! -e src/typescript.svg</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e public/index.legacy.html && test ! -e public/vite.svg && test ! -e src/main.ts && test ! -e src/counter.ts && test ! -e src/style.css && test ! -e src/typescript.svg` exits 0 after deletion.
    - The deletion commit removes `public/index.legacy.html`, `public/vite.svg`, `src/main.ts`, `src/counter.ts`, `src/style.css`, and `src/typescript.svg` from tracked source.
    - `rg -n "index\\.legacy|vite\\.svg|src/main\\.ts|counter\\.ts|typescript\\.svg" index.html src public README.md docs` returns no live references except historical planning docs.
    - `public/styles/registry-explorer.css` remains present.
  </acceptance_criteria>
  <done>Legacy deployed page and unused starter files are absent from tracked app/deployable source.</done>
</task>

<task type="auto">
  <name>Task 2: Update canonical shell title and contributor docs</name>
  <read_first>
    <file>index.html</file>
    <file>README.md</file>
    <file>.gitignore</file>
    <file>src/registry-explorer/entry.ts</file>
    <file>public/styles/registry-explorer.css</file>
    <file>.planning/phases/01-foundation-safety-verification/01-UI-SPEC.md</file>
  </read_first>
  <files>index.html, README.md</files>
  <action>Update `index.html` document title from `Component Registry Explorer` to `Registry Atlas` per the UI-SPEC canonical surface allowance. Update README architecture/contributor guidance to name `index.html`, `public/styles/registry-explorer.css`, and `src/registry-explorer/entry.ts` as the canonical browser app surface. Document that `dist/` is generated output ignored by `.gitignore` and should be regenerated with `pnpm build`, not edited or committed. Keep README lightweight and do not introduce Phase 2 sync, Phase 3 component discovery, or Phase 4 install-action instructions.</action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `index.html` contains `<title>Registry Atlas</title>`.
    - README identifies the canonical app surface files and does not mention `src/main.ts`, `src/counter.ts`, `src/style.css`, `src/typescript.svg`, `public/vite.svg`, or `public/index.legacy.html` as active app files.
    - README states `dist/` is generated output and `.gitignore` already ignores it.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>Contributors and users are pointed at the canonical app surface, and generated output policy is explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public static assets to deployed site | Files under `public/` are copied directly to the deployed output. |
| Contributor docs to implementation behavior | README guidance can send maintainers to live or dead entry points. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | `public/index.legacy.html` | mitigate | Remove the legacy deployable page so it cannot bypass Phase 1 safety fixes. |
| T-01-04-02 | Spoofing | Vite starter assets | mitigate | Remove starter files that can confuse contributors about the active app surface. |
| T-01-04-03 | Tampering | `dist/` generated output | mitigate | Keep `dist/` ignored, document regeneration through `pnpm build`, and do not track generated output. |
</threat_model>

<verification>
Run `pnpm build`. Confirm deletion targets are no longer tracked and `dist/` remains ignored.
</verification>

<success_criteria>
FOUND-03 is complete: only the canonical Registry Atlas static shell, stylesheet, and TypeScript entry remain as the active app surface; stale legacy/starter artifacts are gone.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Canonical deployed surface cleanup | 01-04 | COVERED | Required by Phase 1 goal.
REQ | FOUND-03 | User only reaches canonical app surface | 01-04 | COVERED | Tasks 1 and 2.
CONTEXT | D-05 | Remove stale deployed artifacts | 01-04 | COVERED | Task 1.
CONTEXT | D-06 | `public/index.legacy.html` not deployable | 01-04 | COVERED | Task 1.
CONTEXT | D-07 | Remove unused Vite starter remnants and update docs | 01-04 | COVERED | Tasks 1 and 2.
CONTEXT | D-08 | Make `dist/` policy explicit | 01-04 | COVERED | Task 2.
RESEARCH | - | Canonical surface is `index.html`, stylesheet, entry, core/ui/data | 01-04 | COVERED | Task 2.
UI-SPEC | - | Canonical app surface and title allowance | 01-04 | COVERED | Task 2.
</source_audit>

<output>
Create `.planning/phases/01-foundation-safety-verification/01-04-SUMMARY.md` when done.
</output>
