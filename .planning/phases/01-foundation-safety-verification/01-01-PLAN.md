---
phase: 01-foundation-safety-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.test.json
  - README.md
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
must_haves:
  truths:
    - "Maintainer can run one documented package script for the Registry Atlas test suite per D-01 and FOUND-01."
    - "Maintainer can type-check test files explicitly per D-03."
    - "Build remains the production validation gate while developer test and combined verification scripts are explicit per D-04."
  artifacts:
    - path: "package.json"
      provides: "Vitest-backed test, test:watch, typecheck, typecheck:test, and verify scripts"
      contains: "vitest run"
    - path: "tsconfig.test.json"
      provides: "Strict TypeScript project covering src and tests"
      contains: "tests/**/*.ts"
    - path: "README.md"
      provides: "Documented test and verification commands"
      contains: "pnpm test"
  key_links:
    - from: "package.json"
      to: "tsconfig.test.json"
      via: "typecheck:test script"
      pattern: "tsc --noEmit -p tsconfig.test.json"
    - from: "README.md"
      to: "package.json"
      via: "documented commands"
      pattern: "pnpm test"
---

## Phase Goal

**As a** Registry Atlas maintainer, **I want to** safely evolve the existing app with runnable tests, canonical deployed surface, safe rendering primitives, and one vocabulary source of truth, **so that** later official-directory import work can proceed without silent verification, deployment, rendering, or taxonomy drift.

<objective>
Create the runnable verification baseline for Phase 1. This implements FOUND-01, starts FOUND-02, and honors D-01, D-02, D-03, and D-04 without changing the app architecture.

Purpose: Maintainers need one obvious test command, explicit test type-checking, and an explicit combined verification command before later phases import broader third-party data.
Output: Updated package scripts, Vitest dependency/lockfile state, strict test TypeScript config, and README verification instructions.
</objective>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-safety-verification/01-CONTEXT.md
@.planning/phases/01-foundation-safety-verification/01-RESEARCH.md
@.planning/phases/01-foundation-safety-verification/01-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@package.json
@tsconfig.json
@tests/registry-explorer/grouping.test.ts
@tests/registry-explorer/matrixColumns.test.ts
@README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify Vitest package legitimacy before dependency installation</name>
  <read_first>
    <file>.planning/phases/01-foundation-safety-verification/01-RESEARCH.md</file>
    <file>package.json</file>
    <file>pnpm-lock.yaml</file>
  </read_first>
  <files>package.json, pnpm-lock.yaml</files>
  <action>Before running any package-manager install command, verify the `vitest` npm package because Phase 1 research did not include a Package Legitimacy Audit table. Check `https://www.npmjs.com/package/vitest`, confirm the package name is exactly `vitest`, confirm it is the official Vite/Vitest test runner package, and only then proceed to add it as a dev dependency. This checkpoint exists for the package legitimacy gate; do not install unrelated test runners or add React, Tailwind, shadcn, Jest, Playwright, or Cypress.</action>
  <verify>
    <automated>npm view vitest name version repository.url --json</automated>
  </verify>
  <acceptance_criteria>
    - CLI output reports `"name": "vitest"`.
    - Repository URL or package metadata identifies the official Vitest project.
    - No package-manager install command has run before this checkpoint is approved.
  </acceptance_criteria>
  <done>Human approval confirms `vitest` is legitimate for this repo and the executor may add it as a dev dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add executable test, type-check, and verify scripts</name>
  <read_first>
    <file>package.json</file>
    <file>pnpm-lock.yaml</file>
    <file>tsconfig.json</file>
    <file>tests/registry-explorer/grouping.test.ts</file>
    <file>tests/registry-explorer/matrixColumns.test.ts</file>
    <file>README.md</file>
    <file>.planning/phases/01-foundation-safety-verification/01-CONTEXT.md</file>
    <file>.planning/codebase/TESTING.md</file>
  </read_first>
  <files>package.json, pnpm-lock.yaml, tsconfig.test.json, README.md</files>
  <behavior>
    - Test script: `pnpm test` runs existing Vitest tests once and exits non-watch.
    - Test type-check: `pnpm typecheck:test` type-checks `tests/**/*.ts` and `src/**/*.ts` without weakening strict compiler options.
    - Combined gate: `pnpm verify` runs source type-checking, test type-checking, tests, and production build explicitly.
  </behavior>
  <action>Add `vitest` as a dev dependency after Task 1 approval. Add scripts: `test` as `vitest run`, `test:watch` as `vitest`, `typecheck` as `tsc --noEmit`, `typecheck:test` as `tsc --noEmit -p tsconfig.test.json`, and `verify` as a chained command that runs `pnpm typecheck`, `pnpm typecheck:test`, `pnpm test`, and `pnpm build` in that order. Create `tsconfig.test.json` extending `tsconfig.json`, include `src/**/*.ts` and `tests/**/*.ts`, and add `types: ["vitest/globals", "vite/client"]` only if needed for test compilation. Keep `build` as `tsc && vite build` per D-04. Update README with the developer verification commands and state that `pnpm build` remains the production output check.</action>
  <verify>
    <automated>pnpm typecheck && pnpm typecheck:test && pnpm test && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `test`, `test:watch`, `typecheck`, `typecheck:test`, and `verify` scripts with the exact responsibilities in the action.
    - `package.json` lists `vitest` under `devDependencies` and `pnpm-lock.yaml` contains the resolved lockfile entry.
    - `tsconfig.test.json` exists and includes both source and test TypeScript files.
    - README documents `pnpm test`, `pnpm typecheck:test`, `pnpm verify`, and `pnpm build`.
    - The automated verification command exits 0.
  </acceptance_criteria>
  <done>Maintainer can run one documented test command and one documented combined verification command; existing tests are runnable and type-checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to local dev environment | A new dev dependency enters the toolchain and lockfile. |
| tests to production source | Test TypeScript imports production modules and can mask drift if not type-checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-SC | Tampering | npm install for `vitest` | mitigate | Blocking human package legitimacy checkpoint before dependency installation because RESEARCH.md has no Package Legitimacy Audit table. |
| T-01-01-01 | Tampering | `package.json` scripts | mitigate | Keep `build` unchanged, add explicit scripts, and verify with `pnpm typecheck && pnpm typecheck:test && pnpm test && pnpm build`. |
| T-01-01-02 | Repudiation | Test coverage claims | mitigate | README must document exact commands; `verify` must run test and type-check commands directly. |
</threat_model>

<verification>
Run `pnpm verify`. If that fails, run the failing subcommand directly and fix the root cause before producing the summary.
</verification>

<success_criteria>
FOUND-01 is complete and FOUND-02 has a runnable baseline: `pnpm test`, `pnpm typecheck:test`, and `pnpm verify` exist, are documented, and pass.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Runnable tests and explicit verification baseline | 01-01 | COVERED | Implements the first Phase 1 foundation slice.
REQ | FOUND-01 | One documented package script runs the test suite | 01-01 | COVERED | `pnpm test` and README command.
REQ | FOUND-02 | Automated tests are runnable | 01-01 | COVERED | Existing tests run; later plans extend coverage. Phase 1 establishes the test infrastructure for future command-generation logic while actual install-action command generation remains Phase 4 scope.
CONTEXT | D-01 | Real Vitest-backed test script | 01-01 | COVERED | Task 2.
CONTEXT | D-02 | Baseline covers existing core tests first | 01-01 | COVERED | Task 2 runs existing tests.
CONTEXT | D-03 | Test type-checking explicit | 01-01 | COVERED | `typecheck:test`.
CONTEXT | D-04 | Build remains production gate; combined validation explicit | 01-01 | COVERED | `verify` plus unchanged build.
RESEARCH | - | Add Vitest and explicit scripts | 01-01 | COVERED | Task 2.
</source_audit>

<output>
Create `.planning/phases/01-foundation-safety-verification/01-01-SUMMARY.md` when done.
</output>
