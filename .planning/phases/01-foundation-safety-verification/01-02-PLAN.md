---
phase: 01-foundation-safety-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/registry-explorer/core/registry.schema.ts
  - tests/registry-explorer/matrixColumns.test.ts
  - tests/registry-explorer/registryData.test.ts
  - docs/registry-explorer-data.md
autonomous: true
requirements:
  - FOUND-02
  - FOUND-05
must_haves:
  truths:
    - "Maintainer can update focus and component vocabularies from const arrays without type/runtime drift per D-13 and FOUND-05."
    - "Maintainer can verify matrix columns and production data use only allowed vocabulary values per D-15."
    - "Labels remain in `src/registry-explorer/core/labels.ts` per D-14."
  artifacts:
    - path: "src/registry-explorer/core/registry.schema.ts"
      provides: "Const-array source of truth and derived union types"
      contains: "(typeof PRIMARY_FOCUS_VALUES)[number]"
    - path: "tests/registry-explorer/registryData.test.ts"
      provides: "Production dataset vocabulary and data invariant tests"
      contains: "registries"
    - path: "docs/registry-explorer-data.md"
      provides: "Vocabulary maintenance instructions"
      contains: "PRIMARY_FOCUS_VALUES"
  key_links:
    - from: "src/registry-explorer/core/registry.schema.ts"
      to: "tests/registry-explorer/matrixColumns.test.ts"
      via: "COMPONENT_TAG_VALUES validation"
      pattern: "COMPONENT_TAG_VALUES"
    - from: "src/registry-explorer/data/registries.data.ts"
      to: "tests/registry-explorer/registryData.test.ts"
      via: "production data import"
      pattern: "registries"
---

## Phase Goal

**As a** Registry Atlas maintainer, **I want to** safely evolve the existing app with runnable tests, canonical deployed surface, safe rendering primitives, and one vocabulary source of truth, **so that** later official-directory import work can proceed without silent verification, deployment, rendering, or taxonomy drift.

<objective>
Make controlled vocabulary values the single source of truth and add automated data invariant coverage. This implements FOUND-05, extends FOUND-02, and honors D-13, D-14, D-15, and D-16.

Purpose: Future data sync and component discovery work should not silently drift between TypeScript types, runtime arrays, matrix config, labels, and production registry records.
Output: Const-array-derived schema types, stronger vocabulary/config tests, a production data invariant test, and updated maintenance docs.
</objective>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-safety-verification/01-CONTEXT.md
@.planning/phases/01-foundation-safety-verification/01-RESEARCH.md
@.planning/phases/01-foundation-safety-verification/01-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@src/registry-explorer/core/registry.schema.ts
@src/registry-explorer/core/matrixColumns.ts
@src/registry-explorer/core/labels.ts
@src/registry-explorer/data/registries.data.ts
@tests/registry-explorer/matrixColumns.test.ts
@docs/registry-explorer-data.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing vocabulary and production data invariant tests</name>
  <read_first>
    <file>tests/registry-explorer/matrixColumns.test.ts</file>
    <file>tests/registry-explorer/grouping.test.ts</file>
    <file>src/registry-explorer/core/registry.schema.ts</file>
    <file>src/registry-explorer/core/matrixColumns.ts</file>
    <file>src/registry-explorer/data/registries.data.ts</file>
    <file>.planning/phases/01-foundation-safety-verification/01-CONTEXT.md</file>
  </read_first>
  <files>tests/registry-explorer/matrixColumns.test.ts, tests/registry-explorer/registryData.test.ts</files>
  <behavior>
    - Duplicate detection fails if `PRIMARY_FOCUS_VALUES` or `COMPONENT_TAG_VALUES` contains duplicates.
    - Matrix validation fails if a matrix column is not a valid component tag.
    - Production data validation fails if a registry has an invalid `primary_focus`, invalid `component_tags`, empty name, empty description, duplicate name, or non-HTTPS URL.
  </behavior>
  <action>Extend `matrixColumns.test.ts` with no-duplicate assertions for `PRIMARY_FOCUS_VALUES` and `COMPONENT_TAG_VALUES`, while preserving existing matrix column tests. Create `tests/registry-explorer/registryData.test.ts` using the existing Vitest style, import `registries`, `PRIMARY_FOCUS_VALUES`, and `COMPONENT_TAG_VALUES`, and assert production records use allowed focus and component values. Include current required-known-tag assertions from existing tests and do not add Phase 3 taxonomy expansion.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/matrixColumns.test.ts tests/registry-explorer/registryData.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New tests exist before the schema refactor is completed.
    - Tests assert no duplicate focus values and no duplicate component tag values.
    - Tests import the production `registries` array instead of duplicating production data.
    - Tests include HTTPS-only URL parsing for current production registry URLs without adding runtime sync logic.
  </acceptance_criteria>
  <done>Vocabulary and production data drift risks are captured by automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor schema vocabularies to const arrays as canonical source</name>
  <read_first>
    <file>src/registry-explorer/core/registry.schema.ts</file>
    <file>src/registry-explorer/core/labels.ts</file>
    <file>src/registry-explorer/core/matrixColumns.ts</file>
    <file>tests/registry-explorer/matrixColumns.test.ts</file>
    <file>tests/registry-explorer/registryData.test.ts</file>
    <file>docs/registry-explorer-data.md</file>
  </read_first>
  <files>src/registry-explorer/core/registry.schema.ts, docs/registry-explorer-data.md</files>
  <behavior>
    - `PrimaryFocus` is derived from `PRIMARY_FOCUS_VALUES`.
    - `ComponentTag` is derived from `COMPONENT_TAG_VALUES`.
    - Human-facing labels stay in `labels.ts`.
    - Vocabulary docs tell maintainers to edit the const arrays, not parallel unions.
  </behavior>
  <action>In `registry.schema.ts`, define `PRIMARY_FOCUS_VALUES` first with `as const`, then export `type PrimaryFocus = (typeof PRIMARY_FOCUS_VALUES)[number]`. Define `COMPONENT_TAG_VALUES` first with `as const`, then export `type ComponentTag = (typeof COMPONENT_TAG_VALUES)[number]`. Preserve every current value exactly; do not add major taxonomy values per D-16. Leave `Registry`, group interfaces, and exported names unchanged. Update `docs/registry-explorer-data.md` so vocabulary updates instruct maintainers to edit the const arrays and run `pnpm test`, `pnpm typecheck:test`, and `pnpm verify`.</action>
  <verify>
    <automated>pnpm typecheck && pnpm typecheck:test && pnpm test -- --run tests/registry-explorer/matrixColumns.test.ts tests/registry-explorer/registryData.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `registry.schema.ts` contains `export type PrimaryFocus = (typeof PRIMARY_FOCUS_VALUES)[number]`.
    - `registry.schema.ts` contains `export type ComponentTag = (typeof COMPONENT_TAG_VALUES)[number]`.
    - `labels.ts` remains the location for focus and component label behavior.
    - `docs/registry-explorer-data.md` no longer instructs maintainers to edit both a union type and a values array.
    - Automated verification exits 0.
  </acceptance_criteria>
  <done>Controlled vocabularies have one source of truth, and production data/config invariants are tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static registry data to typed app logic | Hand-maintained or future generated data crosses into grouping, matrix, and rendering logic. |
| Vocabulary config to production data | Runtime arrays determine which values are considered valid. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | `registry.schema.ts` vocabularies | mitigate | Derive union types from const arrays so runtime and compile-time values cannot drift. |
| T-01-02-02 | Tampering | `registries.data.ts` records | mitigate | Add production data tests for duplicate names, HTTPS URLs, non-empty display fields, and allowed vocabulary membership. |
| T-01-02-03 | Information Disclosure | Registry status wording | accept | This plan does not add user-facing provenance or approval labels; Phase 3 handles neutral status indicators. |
</threat_model>

<verification>
Run `pnpm verify`. Confirm the focused vocabulary/data test files are included in `pnpm test` output.
</verification>

<success_criteria>
FOUND-05 is complete and FOUND-02 is stronger: vocabulary changes flow from const arrays to types, matrix config, production data tests, and maintenance docs.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | One vocabulary source of truth | 01-02 | COVERED | Required by Phase 1 goal.
REQ | FOUND-02 | Generated data and grouping assumptions protected by tests | 01-02 | COVERED | Production data invariant tests.
REQ | FOUND-05 | Controlled vocabularies update from a single source | 01-02 | COVERED | Const arrays derive types.
CONTEXT | D-13 | Const arrays are canonical source | 01-02 | COVERED | Task 2.
CONTEXT | D-14 | Keep labels in labels.ts | 01-02 | COVERED | Task 2.
CONTEXT | D-15 | Tests assert vocabulary invariants | 01-02 | COVERED | Task 1.
CONTEXT | D-16 | No major taxonomy expansion | 01-02 | COVERED | Task 2 action forbids expansion.
RESEARCH | - | Add duplicate/value/production-data invariant tests | 01-02 | COVERED | Tasks 1 and 2.
</source_audit>

<output>
Create `.planning/phases/01-foundation-safety-verification/01-02-SUMMARY.md` when done.
</output>
