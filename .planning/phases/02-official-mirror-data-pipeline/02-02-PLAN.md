---
phase: 02-official-mirror-data-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - scripts/validate-registry-data.mjs
  - package.json
  - tests/registry-explorer/registryData.test.ts
  - tests/registry-explorer/registryMirror.test.ts
autonomous: true
requirements:
  - MIRR-03
  - MIRR-04
  - MIRR-06
  - HARD-01
must_haves:
  truths:
    - "A local validation command fails on duplicate, missing, malformed, or de-prefixed namespaces."
    - "A local validation command fails on missing, malformed, or policy-disallowed homepage and registry URL template protocols."
    - "Validation checks controlled vocabulary coverage for inline Atlas enrichment fields."
  artifacts:
    - path: "scripts/validate-registry-data.mjs"
      provides: "Local mirror validation CLI"
      contains: "public/data/registries.json"
    - path: "package.json"
      provides: "Data validation script"
      contains: "validate:data"
    - path: "tests/registry-explorer/registryMirror.test.ts"
      provides: "Pure validation/normalization test coverage"
      contains: "duplicate"
  key_links:
    - from: "scripts/validate-registry-data.mjs"
      to: "public/data/registries.json"
      via: "runtime artifact validation"
      pattern: "registries"
---

## Phase Goal

**As a** Registry Atlas maintainer, **I want to** keep Registry Atlas aligned with the official shadcn registry directory through reviewable sync artifacts and strict validation, **so that** bad generated data cannot silently become accepted app data.

<objective>
Add the strict local validation command for the official mirror artifacts. This implements MIRR-03, MIRR-04, MIRR-06, and HARD-01.

Purpose: Sync intentionally writes artifacts even when data needs review, so validation must be the explicit gate for accepting generated data.
Output: `pnpm validate:data`, validation helper tests, and updated production data invariant tests.
</objective>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-official-mirror-data-pipeline/02-CONTEXT.md
@.planning/phases/02-official-mirror-data-pipeline/02-RESEARCH.md
@.planning/phases/02-official-mirror-data-pipeline/02-PATTERNS.md
@.planning/phases/02-official-mirror-data-pipeline/02-01-SUMMARY.md
@public/data/registries.json
@data/shadcn/registries.raw.json
@src/registry-explorer/core/registry.schema.ts
@tests/registry-explorer/registryData.test.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract mirror validation helpers with focused tests</name>
  <read_first>
    <file>src/registry-explorer/core/registry.schema.ts</file>
    <file>tests/registry-explorer/registryData.test.ts</file>
    <file>.planning/phases/02-official-mirror-data-pipeline/02-CONTEXT.md</file>
    <file>.planning/phases/02-official-mirror-data-pipeline/02-RESEARCH.md</file>
  </read_first>
  <files>src/registry-explorer/core/registryMirror.ts, tests/registry-explorer/registryMirror.test.ts</files>
  <behavior>
    - Validation accepts the normalized mirror shape produced by Plan 02-01.
    - Validation fails duplicate namespaces case-insensitively.
    - Validation fails namespaces missing the leading `@`.
    - Validation fails missing homepage or registry URL template fields.
    - Validation reports HTTP official fields as transport warnings unless the policy constant disallows them.
    - Validation fails `javascript:`, `data:`, malformed URLs, and templates without `{name}`.
    - Validation fails Atlas enrichment values outside `PRIMARY_FOCUS_VALUES` and `COMPONENT_TAG_VALUES`.
  </behavior>
  <action>Create `src/registry-explorer/core/registryMirror.ts` with exported validation helpers such as `validateRegistryMirror`, `validateNamespace`, `validateUrlField`, and `validateRegistryUrlTemplate`. Add `tests/registry-explorer/registryMirror.test.ts` using Vitest. Define the protocol policy explicitly: allow `https:` and `http:` for official directory homepage/template fields, return warnings for `http:`, and fail `javascript:`, `data:`, parse errors, and protocol-relative values. Require registry URL templates to contain `{name}`. Validation result should include `errors` and `warnings` arrays with stable string codes or messages that the CLI can print. Do not label official HTTP entries as unsafe or malicious.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/registryMirror.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `registryMirror.test.ts` includes duplicate namespace, de-prefixed namespace, missing URL, `javascript:`, `data:`, HTTP warning, and invalid vocabulary cases.
    - `src/registry-explorer/core/registryMirror.ts` imports `PRIMARY_FOCUS_VALUES` and `COMPONENT_TAG_VALUES`.
    - HTTP official fields produce warnings and not errors under the default policy.
    - Automated verification exits 0.
  </acceptance_criteria>
  <done>Mirror validation rules are test-covered before the CLI is wired.</done>
</task>

<task type="auto">
  <name>Task 2: Add data validation CLI and package script</name>
  <read_first>
    <file>src/registry-explorer/core/registryMirror.ts</file>
    <file>public/data/registries.json</file>
    <file>data/shadcn/registries.raw.json</file>
    <file>package.json</file>
  </read_first>
  <files>scripts/validate-registry-data.mjs, package.json</files>
  <behavior>
    - `pnpm validate:data` exits 0 when generated artifacts satisfy validation.
    - `pnpm validate:data` exits non-zero when errors are present.
    - CLI output includes error and warning counts.
    - CLI validates raw upstream JSON, normalized runtime JSON, URL policy, namespace policy, and controlled vocabulary coverage.
  </behavior>
  <action>Create `scripts/validate-registry-data.mjs` as an ESM Node script. It must read `data/shadcn/registries.raw.json` and `public/data/registries.json`, call the validation helpers, print a concise summary including `errors: N` and `warnings: N`, print each error/warning with the affected namespace when available, and exit with status 1 if any errors exist. Add `validate:data` to `package.json` as `node scripts/validate-registry-data.mjs`. Do not make validation fetch the network; it validates local artifacts only.</action>
  <verify>
    <automated>pnpm validate:data</automated>
    <automated>pnpm typecheck && pnpm typecheck:test</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `validate:data`.
    - `scripts/validate-registry-data.mjs` reads both `data/shadcn/registries.raw.json` and `public/data/registries.json`.
    - CLI output includes `errors:` and `warnings:`.
    - Validation exits 0 for the current generated official mirror unless true errors remain.
    - Automated verification exits 0.
  </acceptance_criteria>
  <done>Maintainers can run a local strict validation command against generated mirror artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Update production data tests for generated mirror artifacts</name>
  <read_first>
    <file>tests/registry-explorer/registryData.test.ts</file>
    <file>tests/registry-explorer/registryMirror.test.ts</file>
    <file>public/data/registries.json</file>
    <file>src/registry-explorer/core/registryMirror.ts</file>
  </read_first>
  <files>tests/registry-explorer/registryData.test.ts</files>
  <behavior>
    - Production data invariant tests load `public/data/registries.json` instead of the legacy TypeScript array where appropriate.
    - Tests assert current generated mirror count and metadata count agreement.
    - Tests do not fail solely because official records contain HTTP fields that validation treats as warnings.
  </behavior>
  <action>Refactor `tests/registry-explorer/registryData.test.ts` to validate the generated runtime mirror artifact. Keep tests synchronous by reading JSON from disk with Node `fs` APIs if needed. Assert `meta.source_url` is `https://ui.shadcn.com/r/registries.json`, `meta.upstream_count` equals `registries.length`, names are non-empty and unique, all names start with `@`, and inline Atlas enrichment values use allowed controlled vocabularies. Remove the old blanket HTTPS-only assertion because the current official endpoint includes HTTP fields; URL protocol policy is now covered by `registryMirror.test.ts` and `validate:data`.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/registryData.test.ts tests/registry-explorer/registryMirror.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `registryData.test.ts` references `public/data/registries.json`.
    - `registryData.test.ts` no longer asserts every production URL is HTTPS-only.
    - Tests assert all normalized names start with `@`.
    - Automated verification exits 0.
  </acceptance_criteria>
  <done>Production data tests now protect the generated mirror instead of the old hand-maintained data module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated JSON to acceptance gate | Sync writes data before validation decides whether it is acceptable. |
| Official URLs to browser actions | URL fields later become links or registry template inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | Namespace fields | mitigate | Validate non-empty unique leading-`@` namespaces and fail de-prefixed values. |
| T-02-02-02 | Elevation of Privilege | URL protocols | mitigate | Fail scriptable protocols and malformed URLs; warn or allow HTTP by explicit policy. |
| T-02-02-03 | Repudiation | Validation results | mitigate | CLI prints stable error/warning counts and exits non-zero on errors. |
</threat_model>

<verification>
Run `pnpm validate:data`, `pnpm typecheck`, `pnpm typecheck:test`, and `pnpm test -- --run tests/registry-explorer/registryData.test.ts tests/registry-explorer/registryMirror.test.ts`.
</verification>

<success_criteria>
MIRR-03, MIRR-04, MIRR-06, and HARD-01 are covered by a local validation command and focused tests.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | MIRR-03 | Namespace validation failures | 02-02 | COVERED | Validation helper and CLI.
REQ | MIRR-04 | Homepage/template validation failures | 02-02 | COVERED | URL/template helper and CLI.
REQ | MIRR-06 | Official facts vs enrichment | 02-02 | COVERED | Enrichment vocabulary checks.
REQ | HARD-01 | Local validation command | 02-02 | COVERED | `pnpm validate:data`.
CONTEXT | D-10 | Render records with warnings; validate separately | 02-02 | COVERED | Errors/warnings model.
CONTEXT | D-12 | HTTP official entries use neutral language | 02-02 | COVERED | HTTP warnings.
</source_audit>

<output>
Create `.planning/phases/02-official-mirror-data-pipeline/02-02-SUMMARY.md` when done.
</output>
