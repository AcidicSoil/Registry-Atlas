---
phase: 02-official-mirror-data-pipeline
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - package.json
  - README.md
  - docs/registry-explorer-data.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - MIRR-01
  - MIRR-02
  - MIRR-03
  - MIRR-04
  - MIRR-05
  - MIRR-06
  - HARD-01
  - HARD-03
must_haves:
  truths:
    - "The documented maintainer workflow runs sync, validates generated data, and runs the normal verification gate."
    - "`pnpm verify` includes data validation after `validate:data` exists."
    - "Roadmap marks all four Phase 2 plans and keeps Phase 2 requirements traceable."
  artifacts:
    - path: "docs/registry-explorer-data.md"
      provides: "Generated mirror maintenance workflow"
      contains: "pnpm sync:registries"
    - path: "README.md"
      provides: "Contributor verification commands"
      contains: "pnpm validate:data"
    - path: "package.json"
      provides: "Combined verification includes data validation"
      contains: "validate:data"
  key_links:
    - from: "package.json"
      to: "scripts/validate-registry-data.mjs"
      via: "verify script"
      pattern: "pnpm validate:data"
---

## Phase Goal

**As a** Registry Atlas maintainer, **I want to** trust the generated mirror workflow after implementation, **so that** future official-directory refreshes are quick, reviewable, and hard to accept accidentally when invalid.

<objective>
Wire the mirror pipeline into docs and verification. This closes Phase 2 requirements by making the new sync/validate/review workflow discoverable and part of the normal local gate.

Purpose: A mirror pipeline is only maintainable if contributors know which files are generated, how to refresh them, and which command proves data is acceptable.
Output: Updated package verification, README, data maintenance docs, and roadmap plan list.
</objective>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-official-mirror-data-pipeline/02-CONTEXT.md
@.planning/phases/02-official-mirror-data-pipeline/02-RESEARCH.md
@.planning/phases/02-official-mirror-data-pipeline/02-PATTERNS.md
@.planning/phases/02-official-mirror-data-pipeline/02-01-SUMMARY.md
@.planning/phases/02-official-mirror-data-pipeline/02-02-SUMMARY.md
@.planning/phases/02-official-mirror-data-pipeline/02-03-SUMMARY.md
@package.json
@README.md
@docs/registry-explorer-data.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data validation to the combined verify gate</name>
  <read_first>
    <file>package.json</file>
    <file>scripts/validate-registry-data.mjs</file>
    <file>.planning/phases/02-official-mirror-data-pipeline/02-02-SUMMARY.md</file>
  </read_first>
  <files>package.json</files>
  <behavior>
    - `pnpm verify` runs source type-checking, test type-checking, tests, data validation, and production build.
    - Existing `test`, `typecheck`, `typecheck:test`, and `build` scripts remain available.
  </behavior>
  <action>Update the `verify` script in `package.json` to include `pnpm validate:data` after tests and before `pnpm build`, unless implementation evidence shows validation must run earlier. Keep `validate:data` as its own script. Do not remove `sync:registries`; syncing should remain an explicit maintainer action, not part of `verify`.</action>
  <verify>
    <automated>pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `verify` includes `pnpm validate:data`.
    - `package.json` `verify` does not run `pnpm sync:registries`.
    - `pnpm verify` exits 0.
  </acceptance_criteria>
  <done>The standard local gate now checks generated mirror data quality.</done>
</task>

<task type="auto">
  <name>Task 2: Document generated mirror maintenance workflow</name>
  <read_first>
    <file>README.md</file>
    <file>docs/registry-explorer-data.md</file>
    <file>package.json</file>
    <file>data/shadcn/sync-report.json</file>
    <file>public/data/registries.json</file>
  </read_first>
  <files>README.md, docs/registry-explorer-data.md</files>
  <behavior>
    - Docs distinguish raw upstream data, normalized runtime data, and sync report.
    - Docs explain that sync writes artifacts and validation accepts or rejects them.
    - Docs explain inline `official` vs `atlas` field provenance.
    - Docs include the exact commands `pnpm sync:registries`, `pnpm validate:data`, and `pnpm verify`.
  </behavior>
  <action>Update README contributor/verification guidance with `pnpm sync:registries` for refreshing official mirror artifacts, `pnpm validate:data` for local data validation, and `pnpm verify` for the combined gate. Rewrite `docs/registry-explorer-data.md` so it no longer presents `src/registry-explorer/data/registries.data.ts` as the primary maintenance surface. Document `data/shadcn/registries.raw.json`, `data/shadcn/sync-report.json`, and `public/data/registries.json`; document that `official` fields come from shadcn and `atlas` fields are Registry Atlas enrichment; document that valid official links and commands should not be labeled unsafe without a concrete advisory/status reason.</action>
  <verify>
    <automated>rg -n "pnpm sync:registries|pnpm validate:data|public/data/registries.json|data/shadcn/sync-report.json|official|atlas" README.md docs/registry-explorer-data.md</automated>
  </verify>
  <acceptance_criteria>
    - README contains `pnpm validate:data`.
    - `docs/registry-explorer-data.md` contains `pnpm sync:registries`.
    - `docs/registry-explorer-data.md` contains `public/data/registries.json`.
    - `docs/registry-explorer-data.md` distinguishes `official` and `atlas` fields.
    - Verification command finds all listed strings.
  </acceptance_criteria>
  <done>Maintainers have clear instructions for refreshing, reviewing, validating, and verifying mirror data.</done>
</task>

<task type="auto">
  <name>Task 3: Update roadmap plan list and run final Phase 2 checks</name>
  <read_first>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/phases/02-official-mirror-data-pipeline/02-01-PLAN.md</file>
    <file>.planning/phases/02-official-mirror-data-pipeline/02-02-PLAN.md</file>
    <file>.planning/phases/02-official-mirror-data-pipeline/02-03-PLAN.md</file>
    <file>.planning/phases/02-official-mirror-data-pipeline/02-04-PLAN.md</file>
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <behavior>
    - Phase 2 roadmap lists four concrete plans.
    - Final verification proves sync artifacts, validation, tests, and build all work together.
  </behavior>
  <action>Update the Phase 2 `Plans` section in `.planning/ROADMAP.md` from `TBD` to four plans: `02-01-PLAN.md` sync artifacts, `02-02-PLAN.md` validation gate, `02-03-PLAN.md` runtime loading/status UI, and `02-04-PLAN.md` docs/verification wiring. Keep Phase 2 unchecked until execution completes. Run the final verification commands listed below. Do not mark Phase 2 complete during plan execution; that belongs to phase execution and verification workflows.</action>
  <verify>
    <automated>pnpm sync:registries</automated>
    <automated>pnpm validate:data</automated>
    <automated>pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 2 section lists 4 plans.
    - `pnpm sync:registries` exits 0.
    - `pnpm validate:data` exits 0.
    - `pnpm verify` exits 0.
  </acceptance_criteria>
  <done>Phase 2 plans are reflected in roadmap and final local checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated artifacts to contributor workflow | Maintainers decide whether generated data is acceptable. |
| Documentation to future sync behavior | Docs guide future contributors and agents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Repudiation | Data acceptance workflow | mitigate | Document explicit sync, validate, verify sequence and include validation in `verify`. |
| T-02-04-02 | Tampering | Generated data drift | mitigate | `verify` runs `validate:data`; sync remains explicit and reviewable. |
| T-02-04-03 | Information Disclosure | Third-party trust wording | mitigate | Docs avoid implying Registry Atlas certifies third-party code. |
</threat_model>

<verification>
Run `pnpm sync:registries`, `pnpm validate:data`, and `pnpm verify`.
</verification>

<success_criteria>
Phase 2 has a documented, verified maintainer workflow for syncing, reviewing, validating, and consuming the official shadcn registry mirror.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | MIRR-01 | Sync official directory | 02-04 | COVERED | Documented workflow.
REQ | MIRR-02 | Raw/normalized/report artifacts | 02-04 | COVERED | Docs and verify wiring.
REQ | MIRR-03 | Namespace validation | 02-04 | COVERED | `verify` includes validation.
REQ | MIRR-04 | URL/template validation | 02-04 | COVERED | `verify` includes validation.
REQ | MIRR-05 | Mirror count visible and verified | 02-04 | COVERED | Final checks after 02-03.
REQ | MIRR-06 | Provenance/enrichment distinction | 02-04 | COVERED | Docs.
REQ | HARD-01 | Local validation command | 02-04 | COVERED | `validate:data` and `verify`.
REQ | HARD-03 | Review deltas before accepting data | 02-04 | COVERED | Sync report docs.
</source_audit>

<output>
Create `.planning/phases/02-official-mirror-data-pipeline/02-04-SUMMARY.md` when done.
</output>
