---
phase: 02-official-mirror-data-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/registry-explorer/data/loadRegistries.ts
  - src/registry-explorer/entry.ts
  - src/registry-explorer/ui/shell.ts
  - src/registry-explorer/ui/focusView.ts
  - src/registry-explorer/ui/componentView.ts
  - src/registry-explorer/core/grouping.ts
  - public/styles/registry-explorer.css
  - tests/registry-explorer/registryLoader.test.ts
autonomous: true
requirements:
  - MIRR-02
  - MIRR-05
  - MIRR-06
  - HARD-01
must_haves:
  truths:
    - "The browser app loads normalized runtime JSON from a static path instead of importing the legacy TypeScript dataset."
    - "The app shows source URL, sync timestamp, upstream count, local count, and validation/warning status from mirror metadata."
    - "Records with validation warnings remain visible with neutral warning/status language."
  artifacts:
    - path: "src/registry-explorer/data/loadRegistries.ts"
      provides: "Runtime mirror fetch and conversion boundary"
      contains: "import.meta.env.BASE_URL"
    - path: "src/registry-explorer/entry.ts"
      provides: "Async bootstrap for runtime data"
      contains: "loadRegistries"
    - path: "src/registry-explorer/ui/shell.ts"
      provides: "Mirror metadata/status display"
      contains: "upstream"
  key_links:
    - from: "public/data/registries.json"
      to: "src/registry-explorer/data/loadRegistries.ts"
      via: "runtime fetch"
      pattern: "data/registries.json"
---

## Phase Goal

**As a** Registry Atlas user, **I want to** see whether the local mirror matches the official shadcn directory, **so that** I can trust the app is showing current official registry coverage before deeper discovery features arrive.

<objective>
Move the app from static TypeScript data import to runtime normalized JSON loading and display mirror status. This implements the user-visible part of MIRR-02, MIRR-05, MIRR-06, and HARD-01.

Purpose: The official mirror only helps users if the app consumes it and surfaces freshness/count/status clearly.
Output: Runtime loader, async bootstrap, visible mirror metadata, neutral warning states, and loader tests.
</objective>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-official-mirror-data-pipeline/02-CONTEXT.md
@.planning/phases/02-official-mirror-data-pipeline/02-RESEARCH.md
@.planning/phases/02-official-mirror-data-pipeline/02-PATTERNS.md
@.planning/phases/02-official-mirror-data-pipeline/02-02-SUMMARY.md
@public/data/registries.json
@src/registry-explorer/entry.ts
@src/registry-explorer/ui/shell.ts
@src/registry-explorer/ui/renderSafety.ts
@src/registry-explorer/core/grouping.ts
@public/styles/registry-explorer.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime mirror loader and conversion tests</name>
  <read_first>
    <file>public/data/registries.json</file>
    <file>src/registry-explorer/core/registry.schema.ts</file>
    <file>src/registry-explorer/core/registryMirror.ts</file>
    <file>src/registry-explorer/core/grouping.ts</file>
    <file>tests/registry-explorer/grouping.test.ts</file>
  </read_first>
  <files>src/registry-explorer/data/loadRegistries.ts, tests/registry-explorer/registryLoader.test.ts</files>
  <behavior>
    - Loader fetches `${import.meta.env.BASE_URL}data/registries.json`.
    - Loader converts normalized mirror records into the existing `Registry` shape used by grouping/rendering.
    - Loader preserves mirror metadata and warning/status data separately from display registries.
    - Loader handles empty Atlas enrichment without throwing.
  </behavior>
  <action>Create `src/registry-explorer/data/loadRegistries.ts` with exported types for `RegistryMirrorMeta`, `LoadedRegistryData`, and a `loadRegistries(fetchImpl = fetch)` function. Use `import.meta.env.BASE_URL` to build the URL `data/registries.json`. Convert each normalized record into the existing `Registry` shape using display name as the official namespace without the leading `@` only if existing UI needs de-prefixed display; retain the canonical official namespace in metadata or a new field if the schema is extended. Use `atlas.primary_focus` and `atlas.component_tags`, defaulting to empty arrays. Add `tests/registry-explorer/registryLoader.test.ts` with a mocked fetch implementation that verifies URL construction, conversion, metadata preservation, and fetch/parse error behavior. Do not add jsdom or browser E2E for this loader.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/registryLoader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `loadRegistries.ts` contains `import.meta.env.BASE_URL` and `data/registries.json`.
    - Loader tests mock `fetch` and do not require a real network call.
    - Loader output includes both `registries` and `meta`.
    - Loader handles records with empty `atlas.primary_focus` and `atlas.component_tags`.
    - Automated verification exits 0.
  </acceptance_criteria>
  <done>The app has a tested runtime data-loading boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Convert app bootstrap to async runtime loading</name>
  <read_first>
    <file>src/registry-explorer/entry.ts</file>
    <file>src/registry-explorer/data/loadRegistries.ts</file>
    <file>src/registry-explorer/ui/shell.ts</file>
    <file>src/registry-explorer/ui/renderSafety.ts</file>
  </read_first>
  <files>src/registry-explorer/entry.ts, src/registry-explorer/ui/shell.ts</files>
  <behavior>
    - Bootstrap shows a loading state while runtime data is fetched.
    - Bootstrap initializes the explorer with loaded runtime registries and mirror metadata.
    - Fetch or parse failures show a visible data-unavailable state.
    - The legacy static `registries` import is removed from `entry.ts`.
  </behavior>
  <action>Update `src/registry-explorer/entry.ts` so `bootstrap` is async. Remove the import from `./data/registries.data` and import `loadRegistries` from `./data/loadRegistries`. Before awaiting the loader, render concise loading copy into the content body such as `Loading registry mirror...`. On success call `initRegistryExplorer` with loaded `registries` and `mirrorMeta` or equivalent extended options. On failure, log `Registry Explorer: Data load failed` and render a visible error state that says `Registry mirror data is unavailable.` plus `Run pnpm sync:registries and pnpm validate:data, then reload.` Keep the existing missing-DOM-roots error path. Update `ShellOptions` in `shell.ts` to accept mirror metadata and any warning summary needed by Task 3.</action>
  <verify>
    <automated>pnpm typecheck && pnpm typecheck:test && pnpm test -- --run tests/registry-explorer/registryLoader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `entry.ts` no longer imports `./data/registries.data`.
    - `entry.ts` imports `loadRegistries`.
    - `entry.ts` contains `Registry mirror data is unavailable.`
    - `ShellOptions` accepts mirror metadata or an explicit data status object.
    - Automated verification exits 0.
  </acceptance_criteria>
  <done>The browser app consumes the runtime mirror artifact instead of the legacy static module.</done>
</task>

<task type="auto">
  <name>Task 3: Show mirror freshness, counts, and neutral warnings in the UI</name>
  <read_first>
    <file>src/registry-explorer/ui/shell.ts</file>
    <file>src/registry-explorer/ui/focusView.ts</file>
    <file>src/registry-explorer/ui/componentView.ts</file>
    <file>src/registry-explorer/ui/renderSafety.ts</file>
    <file>public/styles/registry-explorer.css</file>
    <file>.planning/phases/02-official-mirror-data-pipeline/02-CONTEXT.md</file>
  </read_first>
  <files>src/registry-explorer/ui/shell.ts, src/registry-explorer/ui/focusView.ts, src/registry-explorer/ui/componentView.ts, public/styles/registry-explorer.css</files>
  <behavior>
    - User can see source URL, sync timestamp, upstream count, and local count.
    - User can see whether local count matches upstream count.
    - Records with warnings remain visible with neutral wording.
    - Valid official links are not described as unsafe; mechanically invalid actions are disabled or rendered unavailable.
  </behavior>
  <action>Add a compact mirror status area to the existing app header/content header path without redesigning the app. Display source label `Official shadcn directory`, source URL as a safe external link, `Synced`, `Upstream`, and `Local` values from mirror metadata, and a count status such as `198 / 198 registries mirrored` when counts match. If warnings exist, display neutral copy such as `Some official fields need review.` and surface warning counts; do not use scare-copy like `unsafe` for valid official entries. Extend registry rows or metadata areas only enough to show warning/status chips when a record has validation warnings. Update CSS with restrained styles matching the current dense explorer surface; do not add cards inside cards, decorative gradients, or a landing page.</action>
  <verify>
    <automated>pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - UI source contains `Official shadcn directory`.
    - UI source contains a count phrase or equivalent computed output using upstream and local counts.
    - Warning copy uses `review` or `warning` language and does not call valid official records unsafe.
    - Existing focus, component, and matrix views still render after `pnpm build`.
    - Automated verification exits 0.
  </acceptance_criteria>
  <done>Users can see mirror freshness/count status and warnings while continuing to browse the registry data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static JSON to browser runtime | Generated JSON crosses into client rendering. |
| Mirror warnings to user action | Validation warnings influence whether links/actions are displayed or disabled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Denial of Service | Runtime JSON fetch | mitigate | Loading and data-unavailable states prevent blank app failure. |
| T-02-03-02 | Spoofing | Source/freshness display | mitigate | Show official source URL, sync timestamp, upstream count, and local count from metadata. |
| T-02-03-03 | Tampering | Warning interpretation | mitigate | Use neutral warning/status language and only disable mechanically invalid fields. |
</threat_model>

<verification>
Run `pnpm typecheck`, `pnpm typecheck:test`, `pnpm test -- --run tests/registry-explorer/registryLoader.test.ts`, and `pnpm build`.
</verification>

<success_criteria>
MIRR-05 is user-visible: the app loads the official mirror runtime JSON and shows source, freshness, upstream count, local count, and warning status.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
REQ | MIRR-02 | Runtime normalized app data consumed | 02-03 | COVERED | Runtime loader and async bootstrap.
REQ | MIRR-05 | User sees mirror count alignment | 02-03 | COVERED | UI metadata/status area.
REQ | MIRR-06 | Official facts vs enrichment visible | 02-03 | PARTIAL | Metadata/status display; richer profiles in Phase 3.
REQ | HARD-01 | URL/validation status reaches app boundary | 02-03 | PARTIAL | Loader and UI warnings; strict CLI in 02-02.
CONTEXT | D-01 | Runtime JSON artifact | 02-03 | COVERED | App fetches `public/data/registries.json`.
CONTEXT | D-10 | Render warnings, not fail closed | 02-03 | COVERED | Warning UI.
CONTEXT | D-11 | Disable only mechanically invalid actions | 02-03 | COVERED | Valid official links remain normal.
</source_audit>

<output>
Create `.planning/phases/02-official-mirror-data-pipeline/02-03-SUMMARY.md` when done.
</output>
