---
phase: 02-official-mirror-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sync-shadcn-registries.mjs
  - data/shadcn/registries.raw.json
  - data/shadcn/sync-report.json
  - public/data/registries.json
  - package.json
autonomous: true
requirements:
  - MIRR-01
  - MIRR-02
  - MIRR-03
  - MIRR-05
  - MIRR-06
  - HARD-03
must_haves:
  truths:
    - "Maintainer can run a package script that fetches `https://ui.shadcn.com/r/registries.json` and writes raw, normalized, and report artifacts."
    - "The normalized runtime JSON preserves leading `@` namespaces and separates official facts from Registry Atlas enrichment fields inline."
    - "The sync report includes source URL, sync timestamp, upstream count, local normalized count, and added/removed/changed namespace deltas."
  artifacts:
    - path: "scripts/sync-shadcn-registries.mjs"
      provides: "Official directory sync pipeline"
      contains: "https://ui.shadcn.com/r/registries.json"
    - path: "public/data/registries.json"
      provides: "Runtime normalized registry mirror"
      contains: "\"registries\""
    - path: "data/shadcn/sync-report.json"
      provides: "Maintainer review report"
      contains: "\"upstream_count\""
  key_links:
    - from: "scripts/sync-shadcn-registries.mjs"
      to: "public/data/registries.json"
      via: "normalization output"
      pattern: "registries"
    - from: "scripts/sync-shadcn-registries.mjs"
      to: "data/shadcn/sync-report.json"
      via: "delta report"
      pattern: "added"
---

## Phase Goal

**As a** Registry Atlas maintainer, **I want to** keep Registry Atlas aligned with the official shadcn registry directory through reviewable sync artifacts and strict validation, **so that** the app can mirror the directory without hand-maintaining all registry records.

<objective>
Create the official directory sync pipeline and first generated artifacts. This implements the write side of MIRR-01, MIRR-02, MIRR-05, MIRR-06, and HARD-03.

Purpose: Maintainers need a repeatable way to fetch the official directory and inspect generated changes before accepting them.
Output: `pnpm sync:registries`, raw upstream JSON, normalized runtime JSON, and a sync report.
</objective>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-official-mirror-data-pipeline/02-CONTEXT.md
@.planning/phases/02-official-mirror-data-pipeline/02-RESEARCH.md
@.planning/phases/02-official-mirror-data-pipeline/02-PATTERNS.md
@src/registry-explorer/data/registries.data.ts
@src/registry-explorer/core/registry.schema.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sync script and package command</name>
  <read_first>
    <file>package.json</file>
    <file>src/registry-explorer/data/registries.data.ts</file>
    <file>src/registry-explorer/core/registry.schema.ts</file>
    <file>.planning/phases/02-official-mirror-data-pipeline/02-CONTEXT.md</file>
    <file>.planning/phases/02-official-mirror-data-pipeline/02-RESEARCH.md</file>
  </read_first>
  <files>scripts/sync-shadcn-registries.mjs, package.json</files>
  <behavior>
    - `pnpm sync:registries` fetches `https://ui.shadcn.com/r/registries.json`.
    - The script accepts the official array shape with `name`, `homepage`, `url`, and `description`.
    - The script preserves leading `@` namespaces and rejects de-prefixed normalized output.
    - The script always writes artifacts after a successful fetch/parse, even if later validation will fail.
  </behavior>
  <action>Create `scripts/sync-shadcn-registries.mjs` as an ESM Node script using built-in `fetch`, `fs/promises`, and `node:path`. Add `sync:registries` to `package.json` as `node scripts/sync-shadcn-registries.mjs`. The script must define constants for source URL `https://ui.shadcn.com/r/registries.json`, raw output `data/shadcn/registries.raw.json`, report output `data/shadcn/sync-report.json`, and runtime output `public/data/registries.json`. It must read existing `public/data/registries.json` when present to preserve inline `atlas` enrichment by exact official namespace. It must create parent directories before writing. Do not add external dependencies, a backend, a database, or scheduled automation.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains a `sync:registries` script that runs `node scripts/sync-shadcn-registries.mjs`.
    - `scripts/sync-shadcn-registries.mjs` contains `https://ui.shadcn.com/r/registries.json`.
    - The script contains output path constants for `data/shadcn/registries.raw.json`, `data/shadcn/sync-report.json`, and `public/data/registries.json`.
    - The script reads existing normalized runtime data before generating new normalized records.
    - Automated verification exits 0.
  </acceptance_criteria>
  <done>The maintainer has a concrete sync command that can write the Phase 2 artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Generate raw, normalized, and report artifacts</name>
  <read_first>
    <file>scripts/sync-shadcn-registries.mjs</file>
    <file>src/registry-explorer/data/registries.data.ts</file>
    <file>.planning/phases/02-official-mirror-data-pipeline/02-RESEARCH.md</file>
  </read_first>
  <files>data/shadcn/registries.raw.json, data/shadcn/sync-report.json, public/data/registries.json</files>
  <behavior>
    - Raw artifact is the fetched official JSON array.
    - Normalized artifact has `meta` and `registries` top-level keys.
    - Each normalized record separates official fields from inline Registry Atlas enrichment.
    - New records without prior enrichment get neutral Atlas defaults.
    - Report includes added, removed, and changed namespace lists.
  </behavior>
  <action>Run `pnpm sync:registries`. Normalize each official record into an object with official facts grouped under `official` using keys `name`, `homepage`, `registry_url_template`, and `description`. Add inline `atlas` enrichment fields using keys `primary_focus`, `component_tags`, `aliases`, `coverage_status`, `confidence`, and `notes`. Preserve existing enrichment for matching names from previous normalized data when available; for the first migration, seed enrichment from `src/registry-explorer/data/registries.data.ts` by matching `@${legacy.name}` or legacy display name where exact matching is possible. New unmatched records must use empty `primary_focus`, empty `component_tags`, empty `aliases`, `coverage_status: "unverified"`, `confidence: "unknown"`, and empty `notes`. The report must compare previous normalized records to fetched records by official `name` and include source URL, `synced_at`, `upstream_count`, `local_count`, `added`, `removed`, and `changed` arrays.</action>
  <verify>
    <automated>pnpm sync:registries</automated>
    <automated>node -e "const fs=require('fs'); const data=JSON.parse(fs.readFileSync('public/data/registries.json','utf8')); if (!Array.isArray(data.registries)) process.exit(1); console.log(data.meta.upstream_count, data.registries.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `data/shadcn/registries.raw.json` exists and parses as a JSON array.
    - `public/data/registries.json` exists and contains top-level `meta` and `registries`.
    - `public/data/registries.json` `meta.source_url` is `https://ui.shadcn.com/r/registries.json`.
    - `public/data/registries.json` `meta.upstream_count` equals the raw upstream array length.
    - Every normalized registry has `official.name` starting with `@`.
    - `data/shadcn/sync-report.json` includes `added`, `removed`, and `changed` arrays.
  </acceptance_criteria>
  <done>Phase 2 has initial reviewable mirror artifacts generated from the official directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official shadcn endpoint to repository artifacts | Remote JSON enters source-controlled raw and normalized data. |
| Existing local enrichment to regenerated records | Local classification metadata is preserved across sync runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Upstream JSON fetch | mitigate | Store raw JSON separately and include source URL plus timestamp in report and runtime metadata. |
| T-02-01-02 | Repudiation | Sync acceptance decision | mitigate | Write sync report with added/removed/changed deltas before data is accepted. |
| T-02-01-03 | Tampering | Local enrichment preservation | mitigate | Preserve enrichment by exact leading-`@` namespace and default unmatched records to neutral unverified values. |
</threat_model>

<verification>
Run `pnpm sync:registries`, `pnpm typecheck`, and the Node artifact shape check from Task 2.
</verification>

<success_criteria>
MIRR-01 and MIRR-02 have a working artifact producer: maintainers can fetch the official directory into raw upstream data, normalized runtime app data, and a review report with current counts and deltas.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Reviewable official mirror sync artifacts | 02-01 | COVERED | Sync script and generated artifacts.
REQ | MIRR-01 | Sync official directory source | 02-01 | COVERED | `sync:registries`.
REQ | MIRR-02 | Store raw, normalized, and report artifacts | 02-01 | COVERED | Task 2.
REQ | MIRR-05 | Upstream/local count available | 02-01 | PARTIAL | Metadata/report counts; UI display in 02-03.
REQ | MIRR-06 | Distinguish official facts from enrichment | 02-01 | COVERED | `official` and `atlas` sections.
REQ | HARD-03 | Review official-directory deltas | 02-01 | COVERED | Sync report.
CONTEXT | D-01 | Runtime normalized JSON artifact | 02-01 | COVERED | `public/data/registries.json`.
CONTEXT | D-04 | Always write artifacts; validate separately | 02-01 | COVERED | Sync behavior.
RESEARCH | - | Official endpoint array of 198 records | 02-01 | COVERED | Source URL and shape.
</source_audit>

<output>
Create `.planning/phases/02-official-mirror-data-pipeline/02-01-SUMMARY.md` when done.
</output>
