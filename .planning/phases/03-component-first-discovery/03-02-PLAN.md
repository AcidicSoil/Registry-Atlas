---
phase: 03-component-first-discovery
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - public/data/registries.json
  - src/registry-explorer/core/registry.schema.ts
  - src/registry-explorer/core/itemRoutes.ts
  - src/registry-explorer/core/index.ts
  - src/registry-explorer/data/loadRegistries.ts
  - tests/registry-explorer/itemRoutes.test.ts
  - tests/registry-explorer/registryLoader.test.ts
autonomous: true
requirements: [DISC-02, DISC-03, DISC-04, DISC-07, HARD-04]
must_haves:
  truths:
    - "Nonzero known item summaries from data/shadcn/registry-items.json flow into public/data/registries.json and loadRegistries before search/profile derivation per D-06a and D-25"
    - "Loaded registries expose itemSummaries with valid slugs, catalog status, source/provenance, and route eligibility instead of placeholder channels"
    - "Direct item routes are valid only for namespace + registry URL template + item slug + allowed http/https resolved URL per D-13 and D-29"
    - "Homepage/source links remain separate from item routes; no homepage can masquerade as a component route per D-12, D-14, and D-15"
  artifacts:
    - path: "public/data/registries.json"
      provides: "Generated runtime mirror with atlas.item_summaries for known catalog-backed items"
    - path: "src/registry-explorer/data/loadRegistries.ts"
      provides: "Runtime mirror conversion preserving official, Atlas, status, and item-summary fields"
    - path: "src/registry-explorer/core/itemRoutes.ts"
      provides: "Pure direct registry item route resolution"
      exports: ["resolveRegistryItemRoute"]
  key_links:
    - from: "public/data/registries.json"
      to: "src/registry-explorer/data/loadRegistries.ts"
      via: "mirror record conversion"
      pattern: "item_summaries|catalog_status|coverage_status|confidence|aliases|notes"
    - from: "src/registry-explorer/data/loadRegistries.ts"
      to: "Registry.itemSummaries"
      via: "loaded app-owned item summary records"
      pattern: "itemSummaries"
    - from: "src/registry-explorer/core/itemRoutes.ts"
      to: "src/registry-explorer/ui/renderSafety.ts"
      via: "same allowed protocol policy"
      pattern: "http:|https:"
---

<objective>
Load populated item summaries into the app model and validate direct item-route resolution.

Purpose: Known catalog-backed item summaries must reach runtime app records before discovery ranking and registry profiles can expose item-level candidates and direct routes per D-06a, D-12, D-13, D-14, D-15, D-16, D-25, D-28, and D-29.
Output: Runtime mirror data containing known item summaries, loader preservation of provenance/status fields, pure item-route resolver, and focused tests.
</objective>

<phase_goal>
**As a** Registry Atlas user, **I want to** start from a component need and compare registry/item candidates, **so that** I can inspect useful options without mistaking partial data for verified coverage.
</phase_goal>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-component-first-discovery/03-CONTEXT.md
@.planning/phases/03-component-first-discovery/03-RESEARCH.md
@.planning/phases/03-component-first-discovery/03-UI-SPEC.md
@.planning/phases/03-component-first-discovery/03-PATTERNS.md
@.planning/phases/03-component-first-discovery/03-01-SUMMARY.md
@public/data/registries.json
@src/registry-explorer/core/registry.schema.ts
@src/registry-explorer/core/registryMirror.ts
@src/registry-explorer/data/loadRegistries.ts
@src/registry-explorer/ui/renderSafety.ts
@tests/registry-explorer/itemRoutes.test.ts
@tests/registry-explorer/registryLoader.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve populated item summaries and provenance in loaded registries</name>
  <files>public/data/registries.json, src/registry-explorer/core/registry.schema.ts, src/registry-explorer/data/loadRegistries.ts, tests/registry-explorer/registryLoader.test.ts</files>
  <read_first>
    @public/data/registries.json
    @src/registry-explorer/core/registry.schema.ts
    @src/registry-explorer/data/loadRegistries.ts
    @tests/registry-explorer/registryLoader.test.ts
    @.planning/phases/03-component-first-discovery/03-01-SUMMARY.md
    @.planning/phases/03-component-first-discovery/03-CONTEXT.md
    @.planning/phases/03-component-first-discovery/03-RESEARCH.md
  </read_first>
  <behavior>
    - `loadRegistries` maps `atlas.aliases`, `atlas.coverage_status`, `atlas.confidence`, `atlas.notes`, and `atlas.catalog_status` into app-owned camelCase fields.
    - `loadRegistries` maps every generated `atlas.item_summaries[]` record into `Registry.itemSummaries[]` with name, slug, optional type/category, source/provenance, catalog status, and route eligibility.
    - A loader fixture includes at least one registry with multiple known item summaries and valid slugs.
    - Missing Atlas arrays default to `[]`, missing item summaries default to `[]`, missing notes default to `''`, missing confidence defaults to `unknown`, missing coverage status defaults to `unverified`, and missing catalog status defaults to `unverified`.
    - Loaded registries preserve `officialName`, `registryUrlTemplate`, `sourceUrl`, `syncedAt`, upstream count, local count, and warnings needed for profiles.
  </behavior>
  <action>Update `Registry` in `registry.schema.ts` with `atlas: { aliases: readonly string[]; coverageStatus: CoverageStatus; confidence: CoverageConfidence; notes: string; catalogStatus: ItemCatalogStatus }`, `itemSummaries: readonly RegistryItemSummary[]`, and expanded `mirror` fields `sourceUrl`, `syncedAt`, `upstreamCount`, and `localCount` while keeping existing `officialName`, `registryUrlTemplate`, and `warnings`. Update `RegistryMirrorRecord` and conversion code in `loadRegistries.ts` to read `atlas.aliases`, `atlas.coverage_status`, `atlas.confidence`, `atlas.notes`, `atlas.catalog_status`, `atlas.item_summaries`, and `status.warnings` from `public/data/registries.json` per D-18 and D-25. Preserve the existing runtime fetch through `${import.meta.env.BASE_URL}data/registries.json`; do not import or restore `src/registry-explorer/data/registries.data.ts` as runtime data and do not perform browser runtime catalog probing. Add loader tests proving a nonzero known item-summary fixture flows from generated mirror shape into loaded `Registry.itemSummaries`.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/registryLoader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/registry-explorer/data/loadRegistries.ts` contains `coverage_status`, `confidence`, `aliases`, `notes`, `catalog_status`, and `item_summaries`.
    - Source assertion: `src/registry-explorer/core/registry.schema.ts` contains `itemSummaries`, `catalogStatus`, `atlas:`, and `mirror:` sections on `Registry`.
    - Behavior assertion: `registryLoader.test.ts` expects `coverageStatus: 'inferred'`, `confidence: 'medium'`, `aliases`, `notes`, `catalogStatus`, `itemSummaries`, `sourceUrl`, `syncedAt`, `upstreamCount`, and `localCount` on a loaded registry fixture.
    - Behavior assertion: `registryLoader.test.ts` proves at least one fixture registry has multiple `itemSummaries` with valid slugs and source/provenance.
  </acceptance_criteria>
  <done>Runtime registry records preserve official, Atlas, status, and known item-summary fields required for search, profiles, route eligibility, and neutral provenance display.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve direct item routes without substituting homepages</name>
  <files>src/registry-explorer/core/itemRoutes.ts, tests/registry-explorer/itemRoutes.test.ts, src/registry-explorer/core/index.ts</files>
  <read_first>
    @src/registry-explorer/core/registryMirror.ts
    @src/registry-explorer/ui/renderSafety.ts
    @src/registry-explorer/core/index.ts
    @.planning/phases/03-component-first-discovery/03-CONTEXT.md
    @.planning/phases/03-component-first-discovery/03-PATTERNS.md
  </read_first>
  <behavior>
    - `resolveRegistryItemRoute('@example', 'https://example.com/r/{name}.json', 'button')` returns a valid `url` ending in `/r/button.json` and label `Open item route`.
    - Missing item slug returns status `missing-item-slug` and label `Item route unavailable`.
    - Missing `{name}` token returns status `invalid-template` and label `Item route unavailable`.
    - `javascript:` or protocol-relative resolved URLs are rejected.
    - A registry homepage is never returned as an item route fallback.
  </behavior>
  <action>Create `itemRoutes.ts` with exported `ResolvedItemRoute` union and `resolveRegistryItemRoute(namespace, registryUrlTemplate, itemSlug)` per D-12, D-13, D-14, D-15, and D-29. Accept item slugs matching lowercase/number/kebab tokens such as `button`, `data-grid`, and `auth-form`; reject blank, whitespace, slash, URL, or command-like tokens. Resolve `{name}` with `encodeURIComponent(itemSlug)`, validate the final URL with the same `http:`/`https:` policy as `toSafeExternalUrl`, and return labels `Open item route`, `Item route unavailable`, or `Catalog not verified` for UI consumers. Export the helper from `src/registry-explorer/core/index.ts`.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/itemRoutes.test.ts</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/registry-explorer/core/itemRoutes.ts` contains no reference to a registry `homepage` fallback and no branch that returns `registry.url` for an item route.
    - Behavior assertion: tests cover valid route resolution, missing item slug, missing template token, invalid slug, disallowed protocol, and protocol-relative URL.
    - CLI assertion: `pnpm typecheck` exits with code 0.
  </acceptance_criteria>
  <done>Direct component/item route eligibility is a pure tested contract and registry homepages cannot masquerade as item routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `public/data/registries.json` -> TypeScript runtime | Generated third-party registry metadata enters app models. |
| URL template + item slug -> external link | Template substitution creates a user-clickable URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | `loadRegistries.ts` | mitigate | Preserve and test generated item summary, catalog, coverage, confidence, and provenance fields before UI use. |
| T-03-02-02 | Spoofing | `itemRoutes.ts` | mitigate | Resolve item routes only from valid namespace/template/slug and allowed http/https URL; never substitute homepage. |
| T-03-02-03 | Information Disclosure | coverage/profile fields | accept | Data is public registry metadata with no user secrets or private state. |
| T-03-02-04 | Elevation of Privilege | item routes | mitigate | Phase 3 creates external links only; no browser-executed installs or command copying. |
| T-03-02-SC | Tampering | npm installs | accept | No new package-manager installs are planned for this phase plan. |
</threat_model>

<verification>
Run `pnpm test -- --run tests/registry-explorer/itemRoutes.test.ts tests/registry-explorer/registryLoader.test.ts`, then `pnpm typecheck`.
</verification>

<success_criteria>
This plan succeeds when nonzero known item summaries reach loaded registry records, route validation is independently tested, and no item-route helper can emit a homepage as a component route.
</success_criteria>

<output>
Create `.planning/phases/03-component-first-discovery/03-02-SUMMARY.md` when done.
</output>
