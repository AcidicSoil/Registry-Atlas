---
phase: 03-component-first-discovery
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - index.html
  - src/registry-explorer/ui/discoveryView.ts
  - src/registry-explorer/ui/registryProfileView.ts
  - src/registry-explorer/ui/index.ts
  - src/registry-explorer/ui/shell.ts
  - public/styles/registry-explorer.css
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-04, DISC-05, DISC-07, HARD-04]
must_haves:
  truths:
    - "The first screen is a usable component-first discovery tool with search and results visible immediately"
    - "Search result rows show matched item/component/capability, registry context, known item summary fields, route state, status/confidence, why-this-matched, and profile/source affordances"
    - "Registry profiles are inspectable from results and preserve official versus Atlas provenance plus known item catalog summaries"
    - "No Phase 4 copy, queue, install execution, or URL share controls appear"
  artifacts:
    - path: "src/registry-explorer/ui/discoveryView.ts"
      provides: "Component-first result and empty/partial state rendering"
      exports: ["renderDiscoveryAside", "renderDiscoveryContent"]
    - path: "src/registry-explorer/ui/registryProfileView.ts"
      provides: "Registry profile rendering"
      exports: ["renderRegistryProfile"]
    - path: "src/registry-explorer/ui/shell.ts"
      provides: "Default discover view and profile selection state"
  key_links:
    - from: "src/registry-explorer/ui/shell.ts"
      to: "src/registry-explorer/core/discovery.ts"
      via: "searchComponentCandidates"
      pattern: "searchComponentCandidates"
    - from: "src/registry-explorer/ui/shell.ts"
      to: "src/registry-explorer/core/registryProfile.ts"
      via: "buildRegistryProfile"
      pattern: "buildRegistryProfile"
---

<objective>
Wire the default component-first discovery and profile experience into the static SPA.

Purpose: Users must be able to start from the existing primary search input, compare compact item-aware candidates, and inspect registry context without large marketing surfaces, false verified coverage, homepage-as-item-route fallbacks, or Phase 4 command actions per D-01 through D-06a, D-09 through D-11, D-12 through D-16, D-17 through D-20, D-25, D-27, and D-28.
Output: Default discover route, dense discovery renderer, profile renderer, shell state/event wiring, and responsive styles.
</objective>

<phase_goal>
**As a** Registry Atlas user, **I want to** start from a component need and compare registry/item candidates, **so that** I can inspect useful options without mistaking partial data for verified coverage.
</phase_goal>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-component-first-discovery/03-CONTEXT.md
@.planning/phases/03-component-first-discovery/03-RESEARCH.md
@.planning/phases/03-component-first-discovery/03-UI-SPEC.md
@.planning/phases/03-component-first-discovery/03-PATTERNS.md
@.planning/phases/03-component-first-discovery/03-03-SUMMARY.md
@index.html
@src/registry-explorer/ui/shell.ts
@src/registry-explorer/ui/componentView.ts
@src/registry-explorer/ui/focusView.ts
@src/registry-explorer/ui/renderSafety.ts
@public/styles/registry-explorer.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make discover the default shell route and keep one primary search input</name>
  <files>index.html, src/registry-explorer/ui/shell.ts</files>
  <read_first>
    @index.html
    @src/registry-explorer/ui/shell.ts
    @src/registry-explorer/core/discovery.ts
    @src/registry-explorer/core/registryProfile.ts
    @.planning/phases/03-component-first-discovery/03-CONTEXT.md
    @.planning/phases/03-component-first-discovery/03-UI-SPEC.md
  </read_first>
  <action>Update `index.html` search input hint text to exactly `Search components, items, registries, or aliases`. Add a visible `data-view="discover"` tab labeled `Discover` with badge `Component search`, keep the existing `focus`, `component`, and `matrix` tabs, and set discover as the active initial tab per D-01, D-02, D-05, and D-21. In `shell.ts`, extend `AppState['currentView']` and `isView` with `discover`, initialize `currentView: 'discover'`, add `selectedCandidateId: string | null` and `selectedProfileRegistryName: string | null`, and call `searchComponentCandidates`/`buildDiscoveryOverview` for the discover route. Keep state closure-local and mutate only through `setState`; do not add backend fetches, URL share state, or framework migration per D-27.</action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `index.html` contains `data-view="discover"` and the exact search input text `Search components, items, registries, or aliases`.
    - Source assertion: `src/registry-explorer/ui/shell.ts` contains `currentView: 'discover'`, `selectedCandidateId`, and `selectedProfileRegistryName`.
    - Source assertion: `src/registry-explorer/ui/shell.ts` contains no `history.pushState`, `localStorage`, backend URL, or server endpoint for Phase 3 state.
  </acceptance_criteria>
  <done>The app opens to a component-first discovery route while preserving the existing single search input and secondary tabs.</done>
</task>

<task type="auto">
  <name>Task 2: Render compact component-first result rows and truthful empty states</name>
  <files>src/registry-explorer/ui/discoveryView.ts, src/registry-explorer/ui/index.ts, src/registry-explorer/ui/shell.ts, public/styles/registry-explorer.css</files>
  <read_first>
    @src/registry-explorer/ui/componentView.ts
    @src/registry-explorer/ui/renderSafety.ts
    @src/registry-explorer/ui/shell.ts
    @src/registry-explorer/core/discovery.ts
    @src/registry-explorer/core/coverageStatus.ts
    @public/styles/registry-explorer.css
    @.planning/phases/03-component-first-discovery/03-UI-SPEC.md
    @.planning/phases/03-component-first-discovery/03-PATTERNS.md
  </read_first>
  <action>Create `discoveryView.ts` exporting `renderDiscoveryAside(root, overview, selectedCandidateId)` and `renderDiscoveryContent(headerRoot, bodyRoot, candidates, overview, searchTerm, selectedCandidateId)`. Rows must show zones in this order from the UI spec: matched item/component/capability label, registry namespace/description, known item slug or `Item slug unknown`, optional item type/category when present, item source/provenance and catalog status, route link `Open item route` only for validated candidate route URLs, otherwise `Item route unavailable` or `Catalog not verified`, coverage status chip, confidence/provenance chip, one `Why this matched` phrase, `View profile` button, and secondary homepage/source links. Use `escapeHtml` for text and `renderExternalLink` for homepage/source/item route links per D-04, D-06a, D-09, D-10, D-11, D-12, D-13, D-14, D-15, and HARD-04. Render homepage/source links only in the secondary affordance zone; never label a homepage/source link as `Open item route`. Render partial-data copy `Some registries do not expose a verified item catalog. Results may include inferred or unverified coverage.` whenever results include non-verified statuses or unavailable/unverified catalog statuses. Add CSS classes for `.discovery-row`, `.discovery-route`, `.discovery-item-meta`, `.catalog-available`, `.catalog-partial`, `.catalog-unavailable`, `.catalog-unverified`, `.status-verified`, `.status-inferred`, `.status-partial`, `.status-unavailable`, `.status-unverified`, and responsive stacked rows using the UI spec color contract.</action>
  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `discoveryView.ts` contains exact strings `View profile`, `Open item route`, `Item route unavailable`, `Catalog not verified`, `Item slug unknown`, `Why this matched`, `No verified item matches yet`, and `No registry or component matches found. Try a component name, capability, namespace, or focus tag.`
    - Source assertion: `discoveryView.ts` uses `escapeHtml` and `renderExternalLink`.
    - Source assertion: `discoveryView.ts` renders `Open item route` only from candidate route success data and never from registry homepage/source link fields.
    - Source assertion: `public/styles/registry-explorer.css` contains `.status-verified`, `.status-inferred`, `.status-partial`, `.status-unavailable`, and `.status-unverified`.
    - CLI assertion: `pnpm build` exits with code 0.
  </acceptance_criteria>
  <done>Users can scan component-first results and see honest route/status/partial-data explanations from the default screen.</done>
</task>

<task type="auto">
  <name>Task 3: Render inspectable registry profiles from result rows</name>
  <files>src/registry-explorer/ui/registryProfileView.ts, src/registry-explorer/ui/index.ts, src/registry-explorer/ui/shell.ts, public/styles/registry-explorer.css</files>
  <read_first>
    @src/registry-explorer/ui/focusView.ts
    @src/registry-explorer/ui/renderSafety.ts
    @src/registry-explorer/ui/shell.ts
    @src/registry-explorer/core/registryProfile.ts
    @.planning/phases/03-component-first-discovery/03-CONTEXT.md
    @.planning/phases/03-component-first-discovery/03-UI-SPEC.md
    @.planning/phases/03-component-first-discovery/03-PATTERNS.md
  </read_first>
  <action>Create `registryProfileView.ts` exporting `renderRegistryProfile(headerRoot, bodyRoot, profile)` and wire `shell.ts` delegated clicks for `[data-profile-registry]` and `[data-back-to-results]`. Profiles must include header fields namespace/name, short description, coverage status, confidence, warning count; sections named exactly `Official shadcn facts`, `Registry Atlas enrichment`, `Item discovery status`, and `Why this matched` when match context exists per D-17, D-18, and D-19. The `Item discovery status` section must render every known item summary row from the profile model with item name, slug, optional type/category, source/provenance, catalog status, route eligibility, and an `Open item route` link only when the profile item row carries a validated item route. Source actions are homepage/source/item route links only; homepage/source links remain separate from item route rows and must never be presented as component/item routes. Do not render `Copy install`, `Copy view`, `Add to queue`, `Batch install`, or URL sharing copy per D-20 and the deferred scope. Long registry URL templates and item routes must wrap using CSS without horizontal overflow.</action>
  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
    <automated>! rg -n "Copy install|Copy view|Add to queue|Batch install|share URL|npx shadcn" src/registry-explorer/ui</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `registryProfileView.ts` contains exact section headings `Official shadcn facts`, `Registry Atlas enrichment`, `Item discovery status`, and `Why this matched`.
    - Source assertion: `registryProfileView.ts` uses `renderExternalLink` for external links and `escapeHtml` for text/facts.
    - Source assertion: `registryProfileView.ts` renders known item summary fields and only uses validated profile item-route data for `Open item route`.
    - Source assertion: `shell.ts` contains delegated selectors `[data-profile-registry]` and `[data-back-to-results]`.
    - CLI assertion: the `rg` command finds none of the out-of-scope Phase 4 copy/action strings in UI source.
  </acceptance_criteria>
  <done>Users can inspect a registry profile from discovery results without losing provenance boundaries or seeing Phase 4 controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User search/profile clicks -> DOM rendering | Untrusted input and generated metadata influence visible HTML. |
| External registry URLs -> browser navigation | Third-party links are exposed to users. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Elevation of Privilege | `discoveryView.ts`, `registryProfileView.ts` | mitigate | Escape all imported text and use safe link helpers for every external URL. |
| T-03-04-02 | Spoofing | status/provenance UI | mitigate | Use exact neutral coverage labels and separate official facts from Atlas enrichment; avoid approval/safety claims. |
| T-03-04-03 | Tampering | item route links | mitigate | Render `Open item route` only from validated candidate/profile item route values and never from homepage/source links. |
| T-03-04-04 | Elevation of Privilege | install behavior | mitigate | Do not execute installs and do not render Phase 4 copy/queue controls in this plan. |
| T-03-04-SC | Tampering | npm installs | accept | No new package-manager installs are planned for this phase plan. |
</threat_model>

<verification>
Run `pnpm typecheck`, `pnpm build`, and `! rg -n "Copy install|Copy view|Add to queue|Batch install|share URL|npx shadcn" src/registry-explorer/ui`.
</verification>

<success_criteria>
This plan succeeds when the default app surface is component-first discovery, result rows are dense and truthful, profiles are inspectable, and all external/generated text remains escaped or safe-linked.
</success_criteria>

<output>
Create `.planning/phases/03-component-first-discovery/03-04-SUMMARY.md` when done.
</output>
