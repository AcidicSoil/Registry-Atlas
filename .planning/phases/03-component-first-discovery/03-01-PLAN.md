---
phase: 03-component-first-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/shadcn/registry-items.json
  - scripts/sync-shadcn-registries.mjs
  - scripts/validate-registry-data.mjs
  - src/registry-explorer/core/registry.schema.ts
  - src/registry-explorer/core/coverageStatus.ts
  - src/registry-explorer/core/registryMirror.ts
  - tests/registry-explorer/coverageStatus.test.ts
  - tests/registry-explorer/registryData.test.ts
  - tests/registry-explorer/registryMirror.test.ts
autonomous: true
requirements: [DISC-02, DISC-04, DISC-07, HARD-04]
must_haves:
  truths:
    - "Coverage status, confidence, catalog status, and item-summary values are typed and validated before discovery ranking per D-06a, D-07, D-08, D-09, and D-28"
    - "data/shadcn/registry-items.json contains nonzero real catalog-backed item summaries for known registry catalogs; zero item summaries is a validation failure when known catalog-backed items are available per D-06a"
    - "Validation rejects malformed item summaries, invalid slugs, unknown statuses, and invented component-tag-only item records"
    - "Registries without known catalogs remain catalogStatus unavailable or unverified instead of becoming verified by inference per D-08 and D-15"
  artifacts:
    - path: "data/shadcn/registry-items.json"
      provides: "Static/generated known item-summary source keyed by registry namespace"
    - path: "src/registry-explorer/core/coverageStatus.ts"
      provides: "Coverage status labels, ordering, and neutral copy"
    - path: "src/registry-explorer/core/registryMirror.ts"
      provides: "Mirror validation for coverage, confidence, catalog status, and item-summary shape"
    - path: "scripts/sync-shadcn-registries.mjs"
      provides: "Generated runtime mirror includes catalog-backed item summaries where known"
  key_links:
    - from: "scripts/sync-shadcn-registries.mjs"
      to: "data/shadcn/registry-items.json"
      via: "static/generated item-summary merge"
      pattern: "registry-items\\.json|item_summaries|catalog_status"
    - from: "scripts/validate-registry-data.mjs"
      to: "public/data/registries.json"
      via: "nonzero known item summary validation"
      pattern: "item_summaries|catalog_status|route_eligible"
    - from: "src/registry-explorer/core/registryMirror.ts"
      to: "src/registry-explorer/core/coverageStatus.ts"
      via: "shared status allowlists"
      pattern: "COVERAGE_STATUS_VALUES|ITEM_CATALOG_STATUS_VALUES"
---

<objective>
Create the validated status and known item-summary data foundation for component-first discovery.

Purpose: Discovery ranking cannot be truthful if Phase 3 only creates an item-summary channel without actual known item summaries. This plan requires catalog-backed item summaries and slugs to exist before downstream search/profile work per D-06a, D-07, D-08, D-09, D-12, D-15, D-25, and D-28.
Output: Status/catalog contracts, item-summary source artifact, sync/validation merge rules, and tests proving nonzero known item summaries are generated and validated.
</objective>

<phase_goal>
**As a** Registry Atlas user, **I want to** start from a component need and compare registry/item candidates, **so that** I can inspect useful options without mistaking partial data for verified coverage.
</phase_goal>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-component-first-discovery/03-CONTEXT.md
@.planning/phases/03-component-first-discovery/03-RESEARCH.md
@.planning/phases/03-component-first-discovery/03-UI-SPEC.md
@.planning/phases/03-component-first-discovery/03-PATTERNS.md
@.planning/phases/02-official-mirror-data-pipeline/02-03-SUMMARY.md
@.planning/phases/02-official-mirror-data-pipeline/02-04-SUMMARY.md
@public/data/registries.json
@data/shadcn/registry-items.json
@scripts/sync-shadcn-registries.mjs
@scripts/validate-registry-data.mjs
@src/registry-explorer/core/registry.schema.ts
@src/registry-explorer/core/registryMirror.ts
@tests/registry-explorer/registryData.test.ts
@tests/registry-explorer/registryMirror.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add coverage/catalog vocabulary and mirror item-summary validation</name>
  <files>src/registry-explorer/core/registry.schema.ts, src/registry-explorer/core/coverageStatus.ts, src/registry-explorer/core/registryMirror.ts, tests/registry-explorer/coverageStatus.test.ts, tests/registry-explorer/registryMirror.test.ts</files>
  <read_first>
    @src/registry-explorer/core/registry.schema.ts
    @src/registry-explorer/core/registryMirror.ts
    @tests/registry-explorer/registryMirror.test.ts
    @.planning/phases/03-component-first-discovery/03-CONTEXT.md
    @.planning/phases/03-component-first-discovery/03-UI-SPEC.md
    @.planning/phases/03-component-first-discovery/03-PATTERNS.md
  </read_first>
  <behavior>
    - `coverageStatusLabel('verified')` returns `Verified item`.
    - `coverageStatusLabel('inferred')` returns `Inferred from Atlas tags`.
    - `coverageStatusLabel('partial')` returns `Partial catalog`.
    - `coverageStatusLabel('unavailable')` returns `Catalog unavailable`.
    - `coverageStatusLabel('unverified')` returns `Unverified coverage`.
    - `compareCoverageStatus('verified', 'unverified')` sorts `verified` ahead of `unverified`.
    - `validateRegistryMirror` errors for unknown `atlas.coverage_status`, unknown `atlas.confidence`, unknown `atlas.catalog_status`, malformed `atlas.item_summaries[]`, invalid item slugs, or `route_eligible: true` without slug/template-validating prerequisites.
  </behavior>
  <action>Add `CoverageStatus = 'verified' | 'inferred' | 'partial' | 'unavailable' | 'unverified'`, `CoverageConfidence = 'high' | 'medium' | 'low' | 'unknown'`, `ItemCatalogStatus = 'available' | 'partial' | 'unavailable' | 'unverified'`, and `RegistryItemSummary` to `registry.schema.ts` per D-06a, D-07, D-08, and D-28. `RegistryItemSummary` must carry `name`, `slug`, optional `type`, optional `category`, `source`, `provenance`, `catalogStatus`, and `routeEligible`. Create `coverageStatus.ts` with exported `COVERAGE_STATUS_VALUES`, `COVERAGE_CONFIDENCE_VALUES`, `ITEM_CATALOG_STATUS_VALUES`, `COVERAGE_STATUS_LABELS`, `COVERAGE_STATUS_ORDER`, `coverageStatusLabel(status)`, `coverageStatusRank(status)`, and `compareCoverageStatus(a, b)`. Extend `MirrorValidationCode` with `atlas-invalid-coverage-status`, `atlas-invalid-confidence`, `atlas-invalid-catalog-status`, `atlas-invalid-item-summary`, and `atlas-invalid-item-slug`; validate `atlas.coverage_status`, `atlas.confidence`, `atlas.catalog_status`, and `atlas.item_summaries[]` through allowlist and slug checks in `registryMirror.ts`. Use exact UI copy from D-09 and the UI spec; do not introduce approval, trusted, certified, secured, or audited language per HARD-04.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/coverageStatus.test.ts tests/registry-explorer/registryMirror.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/registry-explorer/core/coverageStatus.ts` contains all five exact labels `Verified item`, `Inferred from Atlas tags`, `Partial catalog`, `Catalog unavailable`, and `Unverified coverage`.
    - Source assertion: `src/registry-explorer/core/registryMirror.ts` contains `atlas-invalid-coverage-status`, `atlas-invalid-confidence`, `atlas-invalid-catalog-status`, `atlas-invalid-item-summary`, and `atlas-invalid-item-slug`.
    - Behavior assertion: targeted Vitest coverage proves valid and invalid coverage/confidence values, catalog statuses, item summary shapes, item slugs, and route-eligible gating.
  </acceptance_criteria>
  <done>Coverage status, confidence, catalog status, and item summaries are typed, labeled, ordered, and validated without implying third-party code approval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate known catalog-backed item summaries in generated data</name>
  <files>data/shadcn/registry-items.json, scripts/sync-shadcn-registries.mjs, scripts/validate-registry-data.mjs, tests/registry-explorer/registryData.test.ts</files>
  <read_first>
    @data/shadcn/registry-items.json
    @scripts/sync-shadcn-registries.mjs
    @scripts/validate-registry-data.mjs
    @tests/registry-explorer/registryData.test.ts
    @public/data/registries.json
    @data/shadcn/registries.raw.json
    @.planning/phases/03-component-first-discovery/03-CONTEXT.md
    @.planning/phases/03-component-first-discovery/03-RESEARCH.md
  </read_first>
  <behavior>
    - `data/shadcn/registry-items.json` contains at least one known registry namespace with multiple real catalog-backed item summaries and valid slugs.
    - `scripts/sync-shadcn-registries.mjs` merges those summaries into matching `public/data/registries.json` records as `atlas.item_summaries` and sets `atlas.catalog_status` honestly.
    - `scripts/validate-registry-data.mjs` fails when known catalog-backed item input exists but generated `atlas.item_summaries` is empty.
    - Registries without known catalog-backed item input remain `catalog_status: 'unavailable'` or `catalog_status: 'unverified'`; they do not become `verified` or route-eligible from Atlas component tags alone.
    - Tests include at least one registry fixture with multiple known item summaries and valid slugs.
  </behavior>
  <action>Create or update `data/shadcn/registry-items.json` as the checked-in static/generated item-summary source keyed by registry namespace. The artifact must contain real catalog-backed entries for known registry items available to the pipeline, including at least one registry fixture with multiple item summaries and valid slugs; use explicit empty arrays or omitted keys for registries with no known catalog and do not invent item names from component tags. Update `scripts/sync-shadcn-registries.mjs` to merge this source into each generated record as `atlas.item_summaries` with `name`, `slug`, optional `type`, optional `category`, `source`, `provenance`, `catalog_status`, and `route_eligible`, and to set `atlas.catalog_status` to `available` or `partial` only when catalog-backed summaries exist. Update `scripts/validate-registry-data.mjs` and `tests/registry-explorer/registryData.test.ts` so `pnpm validate:data` fails if known catalog-backed item input exists but generated item summaries are empty, if an item slug is invalid, if an item lacks source/provenance, or if a registry is marked verified/route-eligible without slug/template validation prerequisites. Keep browser runtime catalog probing out of scope and do not fetch registry catalogs from app code.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/registryData.test.ts</automated>
    <automated>pnpm validate:data</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `data/shadcn/registry-items.json` exists and contains nonzero item records with valid `slug` values for at least one known registry, including one namespace with multiple item summaries.
    - Source assertion: `scripts/sync-shadcn-registries.mjs` contains `registry-items.json`, `item_summaries`, `catalog_status`, and no browser/runtime fetch code for item catalogs.
    - Source assertion: `scripts/validate-registry-data.mjs` contains a check that fails when generated item summaries are empty while known catalog-backed item input exists.
    - Data assertion: generated `public/data/registries.json` can contain `atlas.item_summaries` with `name`, `slug`, `source`, `provenance`, `catalog_status`, and `route_eligible`, and the generated artifact is not allowed to pass with zero item summaries when known catalog-backed items are available.
  </acceptance_criteria>
  <done>Known catalog-backed item summaries are populated into generated data and validated as nonempty before downstream discovery ranking can pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/shadcn/registry-items.json` -> sync/validation scripts | Static/generated item metadata enters generated runtime data. |
| `public/data/registries.json` -> TypeScript runtime | Generated third-party registry metadata enters app models. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `registry-items.json`, `sync-shadcn-registries.mjs`, `validate-registry-data.mjs` | mitigate | Validate nonzero known item summaries, slug shape, catalog status, source/provenance, and route eligibility before generated data can pass. |
| T-03-01-02 | Spoofing | coverage/status vocabulary | mitigate | Use neutral labels and keep inferred/unverified records lower without hiding them. |
| T-03-01-03 | Repudiation | item-summary provenance | mitigate | Require source/provenance per item summary so known catalog-backed entries are traceable. |
| T-03-01-04 | Elevation of Privilege | generated item metadata | mitigate | Phase 3 stores metadata and links only; it does not execute installs or copy commands. |
| T-03-01-SC | Tampering | npm installs | accept | No new package-manager installs are planned for this phase plan. |
</threat_model>

<verification>
Run `pnpm test -- --run tests/registry-explorer/coverageStatus.test.ts tests/registry-explorer/registryMirror.test.ts tests/registry-explorer/registryData.test.ts`, then `pnpm validate:data`.
</verification>

<success_criteria>
This plan succeeds when status/catalog contracts exist, known catalog-backed item summaries are populated into generated data, validation fails on empty known item-summary output, and registries without known catalogs remain honestly unavailable or unverified.
</success_criteria>

<output>
Create `.planning/phases/03-component-first-discovery/03-01-SUMMARY.md` when done.
</output>
