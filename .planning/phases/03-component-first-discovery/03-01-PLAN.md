---
phase: 03-component-first-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/registry-explorer/core/registry.schema.ts
  - src/registry-explorer/core/coverageStatus.ts
  - src/registry-explorer/core/itemRoutes.ts
  - src/registry-explorer/core/registryMirror.ts
  - src/registry-explorer/data/loadRegistries.ts
  - tests/registry-explorer/coverageStatus.test.ts
  - tests/registry-explorer/itemRoutes.test.ts
  - tests/registry-explorer/registryMirror.test.ts
  - tests/registry-explorer/registryLoader.test.ts
autonomous: true
requirements: [DISC-02, DISC-03, DISC-04, DISC-07, HARD-04]
must_haves:
  truths:
    - "User-facing discovery data preserves coverage status, confidence, aliases, notes, warnings, official facts, and mirror metadata from public/data/registries.json"
    - "Validated item routes exist only for valid namespace + valid registry URL template + valid item slug + allowed http/https resolved URL"
    - "Coverage statuses render from one explicit vocabulary: verified, inferred, partial, unavailable, unverified"
  artifacts:
    - path: "src/registry-explorer/core/coverageStatus.ts"
      provides: "Coverage status labels, ordering, and neutral copy"
    - path: "src/registry-explorer/core/itemRoutes.ts"
      provides: "Pure direct registry item route resolution"
      exports: ["resolveRegistryItemRoute"]
    - path: "src/registry-explorer/data/loadRegistries.ts"
      provides: "Runtime mirror conversion preserving official, atlas, and status fields"
  key_links:
    - from: "src/registry-explorer/data/loadRegistries.ts"
      to: "public/data/registries.json"
      via: "mirror record conversion"
      pattern: "coverage_status|confidence|aliases|notes"
    - from: "src/registry-explorer/core/itemRoutes.ts"
      to: "src/registry-explorer/ui/renderSafety.ts"
      via: "same allowed protocol policy"
      pattern: "http:|https:"
---

<objective>
Create the trustworthy data and route foundation for component-first discovery.

Purpose: Phase 3 results and profiles need item/coverage/status facts before rendering; otherwise the UI cannot distinguish verified, inferred, partial, unavailable, and unverified coverage per D-07, D-08, D-12, D-13, D-15, D-25, D-28, and D-29.
Output: Shared coverage/status contracts, validated item-route resolver, mirror validation extensions, loader preservation of provenance fields, and focused tests.
</objective>

<phase_goal>
**As a** Registry Atlas user, **I want to** start from a component need and compare registry/item candidates, **so that** I can inspect useful options without mistaking partial data for verified coverage.
</phase_goal>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-component-first-discovery/03-CONTEXT.md
@.planning/phases/03-component-first-discovery/03-RESEARCH.md
@.planning/phases/03-component-first-discovery/03-UI-SPEC.md
@.planning/phases/03-component-first-discovery/03-PATTERNS.md
@.planning/phases/02-official-mirror-data-pipeline/02-03-SUMMARY.md
@.planning/phases/02-official-mirror-data-pipeline/02-04-SUMMARY.md
@public/data/registries.json
@src/registry-explorer/core/registry.schema.ts
@src/registry-explorer/core/registryMirror.ts
@src/registry-explorer/data/loadRegistries.ts
@src/registry-explorer/ui/renderSafety.ts
@tests/registry-explorer/registryLoader.test.ts
@tests/registry-explorer/registryMirror.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add coverage status vocabulary and mirror validation</name>
  <files>src/registry-explorer/core/registry.schema.ts, src/registry-explorer/core/coverageStatus.ts, src/registry-explorer/core/registryMirror.ts, tests/registry-explorer/coverageStatus.test.ts, tests/registry-explorer/registryMirror.test.ts</files>
  <read_first>
    @src/registry-explorer/core/registry.schema.ts
    @src/registry-explorer/core/registryMirror.ts
    @tests/registry-explorer/registryMirror.test.ts
    @.planning/phases/03-component-first-discovery/03-CONTEXT.md
    @.planning/phases/03-component-first-discovery/03-UI-SPEC.md
    @.planning/phases/03-component-first-discovery/03-PATTERNS.md
  </read_first>
  <behavior>
    - `coverageStatusLabel('verified')` returns `Verified item`.
    - `coverageStatusLabel('inferred')` returns `Inferred from Atlas tags`.
    - `coverageStatusLabel('partial')` returns `Partial catalog`.
    - `coverageStatusLabel('unavailable')` returns `Catalog unavailable`.
    - `coverageStatusLabel('unverified')` returns `Unverified coverage`.
    - `compareCoverageStatus('verified', 'unverified')` sorts `verified` ahead of `unverified`.
    - `validateRegistryMirror` errors for an unknown `atlas.coverage_status` and unknown `atlas.confidence`.
  </behavior>
  <action>Add `CoverageStatus = 'verified' | 'inferred' | 'partial' | 'unavailable' | 'unverified'` and `CoverageConfidence = 'high' | 'medium' | 'low' | 'unknown'` to `registry.schema.ts` per D-07 and D-08. Create `coverageStatus.ts` with exported `COVERAGE_STATUS_VALUES`, `COVERAGE_CONFIDENCE_VALUES`, `COVERAGE_STATUS_LABELS`, `COVERAGE_STATUS_ORDER`, `coverageStatusLabel(status)`, `coverageStatusRank(status)`, and `compareCoverageStatus(a, b)`. Extend `MirrorValidationCode` with `atlas-invalid-coverage-status` and `atlas-invalid-confidence`; validate `atlas.coverage_status` and `atlas.confidence` through allowlist sets in `registryMirror.ts` using the existing `validateAtlasValues` pattern. Use exact UI copy from D-09 and the UI spec; do not introduce approval, trusted, certified, secured, or audited language per HARD-04.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/coverageStatus.test.ts tests/registry-explorer/registryMirror.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/registry-explorer/core/coverageStatus.ts` contains all five exact labels `Verified item`, `Inferred from Atlas tags`, `Partial catalog`, `Catalog unavailable`, and `Unverified coverage`.
    - Source assertion: `src/registry-explorer/core/registryMirror.ts` contains `atlas-invalid-coverage-status` and `atlas-invalid-confidence`.
    - Behavior assertion: the targeted Vitest command passes and includes tests for valid and invalid coverage/confidence values.
  </acceptance_criteria>
  <done>Coverage status and confidence are typed, labeled, ordered, and validated without implying third-party code approval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve official, Atlas, and status provenance in loaded registries</name>
  <files>src/registry-explorer/core/registry.schema.ts, src/registry-explorer/data/loadRegistries.ts, tests/registry-explorer/registryLoader.test.ts</files>
  <read_first>
    @src/registry-explorer/core/registry.schema.ts
    @src/registry-explorer/data/loadRegistries.ts
    @tests/registry-explorer/registryLoader.test.ts
    @public/data/registries.json
    @.planning/phases/02-official-mirror-data-pipeline/02-03-SUMMARY.md
    @.planning/phases/03-component-first-discovery/03-CONTEXT.md
  </read_first>
  <behavior>
    - `loadRegistries` maps `atlas.aliases`, `atlas.coverage_status`, `atlas.confidence`, and `atlas.notes` into app-owned camelCase fields.
    - Loaded registries preserve `officialName`, `registryUrlTemplate`, `sourceUrl`, `syncedAt`, and warnings needed for profiles.
    - Missing Atlas arrays default to `[]`, missing notes default to `''`, missing confidence defaults to `unknown`, and missing coverage status defaults to `unverified`.
  </behavior>
  <action>Extend `Registry` in `registry.schema.ts` with `atlas: { aliases: readonly string[]; coverageStatus: CoverageStatus; confidence: CoverageConfidence; notes: string }` and expand `mirror` with `sourceUrl`, `syncedAt`, `upstreamCount`, and `localCount` while keeping existing `officialName`, `registryUrlTemplate`, and `warnings`. Update `RegistryMirrorRecord` in `loadRegistries.ts` to read `atlas.aliases`, `atlas.coverage_status`, `atlas.confidence`, `atlas.notes`, and `status.warnings` from `public/data/registries.json` per D-18 and D-25. Preserve the existing runtime fetch through `${import.meta.env.BASE_URL}data/registries.json`; do not import or restore `src/registry-explorer/data/registries.data.ts` as runtime data.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/registryLoader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/registry-explorer/data/loadRegistries.ts` contains `coverage_status`, `confidence`, `aliases`, and `notes`.
    - Source assertion: `src/registry-explorer/core/registry.schema.ts` contains `atlas:` and `mirror:` sections on `Registry`.
    - Behavior assertion: `registryLoader.test.ts` expects `coverageStatus: 'inferred'`, `confidence: 'medium'`, `aliases`, `notes`, `sourceUrl`, `syncedAt`, `upstreamCount`, and `localCount` on a loaded registry fixture.
  </acceptance_criteria>
  <done>Runtime registry records preserve the official/Atlas/status fields required for search, profiles, and neutral provenance display.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resolve direct item routes without substituting homepages</name>
  <files>src/registry-explorer/core/itemRoutes.ts, tests/registry-explorer/itemRoutes.test.ts, src/registry-explorer/core/index.ts</files>
  <read_first>
    @src/registry-explorer/core/registryMirror.ts
    @src/registry-explorer/ui/renderSafety.ts
    @src/registry-explorer/core/index.ts
    @.planning/phases/03-component-first-discovery/03-CONTEXT.md
    @.planning/phases/03-component-first-discovery/03-PATTERNS.md
  </read_first>
  <behavior>
    - `resolveRegistryItemRoute('@example', 'https://example.com/r/{name}.json', 'button')` returns a valid `url` ending in `/r/button.json` and label `Open item route`.
    - Missing item slug returns status `missing-item-slug` and label `Item route unavailable`.
    - Missing `{name}` token returns status `invalid-template` and label `Item route unavailable`.
    - `javascript:` or protocol-relative resolved URLs are rejected.
    - A registry homepage is never returned as an item route fallback.
  </behavior>
  <action>Create `itemRoutes.ts` with exported `ResolvedItemRoute` union and `resolveRegistryItemRoute(namespace, registryUrlTemplate, itemSlug)` per D-12, D-13, D-14, D-15, and D-29. Accept item slugs matching lowercase/number/kebab tokens such as `button`, `data-grid`, and `auth-form`; reject blank, whitespace, slash, URL, or command-like tokens. Resolve `{name}` with `encodeURIComponent(itemSlug)`, validate the final URL with the same `http:`/`https:` policy as `toSafeExternalUrl`, and return labels `Open item route`, `Item route unavailable`, or `Catalog not verified` for UI consumers. Export the helper from `src/registry-explorer/core/index.ts`.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/itemRoutes.test.ts</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/registry-explorer/core/itemRoutes.ts` contains no reference to a registry `homepage` fallback.
    - Behavior assertion: tests cover valid route resolution, missing item slug, missing template token, invalid slug, disallowed protocol, and protocol-relative URL.
    - CLI assertion: `pnpm typecheck` exits with code 0.
  </acceptance_criteria>
  <done>Direct component/item route eligibility is a pure tested contract and registry homepages cannot masquerade as item routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `public/data/registries.json` -> TypeScript runtime | Generated third-party registry metadata enters app models. |
| URL template + item slug -> external link | Template substitution creates a user-clickable URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `loadRegistries.ts` | mitigate | Preserve and validate coverage/confidence fields from generated data before UI use. |
| T-03-01-02 | Spoofing | `itemRoutes.ts` | mitigate | Resolve item routes only from valid namespace/template/slug and allowed http/https URL; never substitute homepage. |
| T-03-01-03 | Information Disclosure | coverage/profile fields | accept | Data is public registry metadata with no user secrets or private state. |
| T-03-01-04 | Elevation of Privilege | item routes | mitigate | Phase 3 creates external links only; no browser-executed installs or command copying. |
| T-03-01-SC | Tampering | npm installs | accept | No new package-manager installs are planned for this phase plan. |
</threat_model>

<verification>
Run `pnpm test -- --run tests/registry-explorer/coverageStatus.test.ts tests/registry-explorer/itemRoutes.test.ts tests/registry-explorer/registryMirror.test.ts tests/registry-explorer/registryLoader.test.ts`, then `pnpm typecheck`.
</verification>

<success_criteria>
This plan succeeds when loaded registry records expose status/provenance fields, coverage labels use exact neutral copy, route validation is independently tested, and no item-route helper can emit a homepage as a component route.
</success_criteria>

<output>
Create `.planning/phases/03-component-first-discovery/03-01-SUMMARY.md` when done.
</output>
