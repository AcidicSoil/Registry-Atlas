---
phase: 03-component-first-discovery
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - src/registry-explorer/core/grouping.ts
  - src/registry-explorer/core/registry.schema.ts
  - src/registry-explorer/ui/focusView.ts
  - src/registry-explorer/ui/componentView.ts
  - src/registry-explorer/ui/matrixView.ts
  - src/registry-explorer/ui/shell.ts
  - public/styles/registry-explorer.css
  - tests/registry-explorer/grouping.test.ts
autonomous: true
requirements: [DISC-01, DISC-04, DISC-05, DISC-06, DISC-07, HARD-04]
must_haves:
  truths:
    - "Focus, component, and matrix tabs remain available as secondary comparison modes"
    - "Secondary views show coverage/status hints instead of binary or registry-only conclusions"
    - "Component view acts as a component index/facet that can feed the primary discovery flow"
    - "Empty and partial-data states explain unavailable or unverified catalog coverage"
  artifacts:
    - path: "src/registry-explorer/core/grouping.ts"
      provides: "Status-aware grouping and matrix row data"
    - path: "src/registry-explorer/ui/focusView.ts"
      provides: "Focus browsing with status distribution hints"
    - path: "src/registry-explorer/ui/componentView.ts"
      provides: "Component facet/index linked to discovery candidates"
    - path: "src/registry-explorer/ui/matrixView.ts"
      provides: "Status-aware matrix cells"
  key_links:
    - from: "src/registry-explorer/ui/componentView.ts"
      to: "src/registry-explorer/ui/shell.ts"
      via: "component selection routes to discover search"
      pattern: "data-discover-component"
    - from: "src/registry-explorer/core/grouping.ts"
      to: "src/registry-explorer/core/coverageStatus.ts"
      via: "status distributions"
      pattern: "coverageStatus"
---

<objective>
Upgrade secondary browse modes so they support component-first discovery and honest coverage status.

Purpose: Phase 3 must preserve focus/component/matrix browsing while avoiding misleading binary coverage or dead-end registry homepage lists per D-21, D-22, D-23, D-24, D-07 through D-11, and DISC-06.
Output: Status-aware grouping/matrix data, secondary renderer updates, shell affordances to jump from facets to discovery, CSS, and tests.
</objective>

<phase_goal>
**As a** Registry Atlas user, **I want to** start from a component need and compare registry/item candidates, **so that** I can inspect useful options without mistaking partial data for verified coverage.
</phase_goal>

<execution_context>
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/workflows/execute-plan.md
@/home/user/projects/temp/ai-apps/.personal-projects/registry-atlas/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-component-first-discovery/03-CONTEXT.md
@.planning/phases/03-component-first-discovery/03-RESEARCH.md
@.planning/phases/03-component-first-discovery/03-UI-SPEC.md
@.planning/phases/03-component-first-discovery/03-PATTERNS.md
@.planning/phases/03-component-first-discovery/03-04-SUMMARY.md
@src/registry-explorer/core/grouping.ts
@src/registry-explorer/core/coverageStatus.ts
@src/registry-explorer/ui/focusView.ts
@src/registry-explorer/ui/componentView.ts
@src/registry-explorer/ui/matrixView.ts
@src/registry-explorer/ui/shell.ts
@public/styles/registry-explorer.css
@tests/registry-explorer/grouping.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add status-aware grouping and matrix view models</name>
  <files>src/registry-explorer/core/registry.schema.ts, src/registry-explorer/core/grouping.ts, tests/registry-explorer/grouping.test.ts</files>
  <read_first>
    @src/registry-explorer/core/registry.schema.ts
    @src/registry-explorer/core/grouping.ts
    @src/registry-explorer/core/coverageStatus.ts
    @tests/registry-explorer/grouping.test.ts
    @.planning/phases/03-component-first-discovery/03-CONTEXT.md
    @.planning/phases/03-component-first-discovery/03-PATTERNS.md
  </read_first>
  <behavior>
    - Focus groups expose a `statusCounts` object with keys for `verified`, `inferred`, `partial`, `unavailable`, and `unverified`.
    - Component groups expose the same `statusCounts` and keep registries sorted deterministically.
    - Matrix rows expose status-aware cell values instead of only `boolean[]`.
    - Existing grouping tests for filtering, counts, and sorting continue to pass.
  </behavior>
  <action>Extend `FocusGroup`, `ComponentGroup`, and `MatrixRow` in `registry.schema.ts` with status-aware fields while preserving existing names used by renderers. Add a reusable `countCoverageStatuses(registries)` helper in `grouping.ts`. Change `buildMatrixRows` to return matrix cells that distinguish `verified`, `inferred`, `partial`, `unavailable`, `unverified`, and no known tag match, so unknown/unverified cells are visually distinct from confirmed absence per D-23. Keep pure helper behavior non-throwing for empty searches and partial records.</action>
  <verify>
    <automated>pnpm test -- --run tests/registry-explorer/grouping.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `src/registry-explorer/core/grouping.ts` contains `countCoverageStatuses`.
    - Behavior assertion: `grouping.test.ts` expects `statusCounts.inferred`, `statusCounts.unverified`, and at least one matrix cell status value.
    - Behavior assertion: existing `buildFocusGroups`, `buildComponentGroups`, `buildMatrixRows`, and `computeMetrics` tests pass.
  </acceptance_criteria>
  <done>Secondary view data contains status distribution and matrix status semantics instead of binary-only coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Render status-aware focus, component, and matrix secondary views</name>
  <files>src/registry-explorer/ui/focusView.ts, src/registry-explorer/ui/componentView.ts, src/registry-explorer/ui/matrixView.ts, public/styles/registry-explorer.css</files>
  <read_first>
    @src/registry-explorer/ui/focusView.ts
    @src/registry-explorer/ui/componentView.ts
    @src/registry-explorer/ui/matrixView.ts
    @src/registry-explorer/ui/renderSafety.ts
    @src/registry-explorer/core/grouping.ts
    @src/registry-explorer/core/coverageStatus.ts
    @public/styles/registry-explorer.css
    @.planning/phases/03-component-first-discovery/03-UI-SPEC.md
  </read_first>
  <action>Update focus view to show compact status distribution hints for each group and status chips on registry rows when groups contain inferred/unverified records per D-24. Update component view into a component index/facet: selected component content must use the component label, show status counts, and include a button or in-app affordance labeled `Search this component` with `data-discover-component` instead of only listing registry homepages per D-22. Update matrix view cells to render status labels/classes for `verified`, `inferred`, `partial`, `unavailable`, and `unverified`; unknown/unverified cells must not share the same styling as confirmed absence per D-23. Empty states must use the UI spec copy for partial data and no matches per D-10 and D-11. Use existing `escapeHtml` and `renderExternalLink` helpers.</action>
  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `componentView.ts` contains `Search this component` and `data-discover-component`.
    - Source assertion: `matrixView.ts` contains all five status class names or status labels for `verified`, `inferred`, `partial`, `unavailable`, and `unverified`.
    - Source assertion: `focusView.ts`, `componentView.ts`, and `matrixView.ts` each import or use status label/count data and continue to use `escapeHtml`.
    - CLI assertion: `pnpm build` exits with code 0.
  </acceptance_criteria>
  <done>Secondary views remain useful comparison modes and display partial/unverified coverage truthfully.</done>
</task>

<task type="auto">
  <name>Task 3: Wire component facets back into primary discovery and run full verification</name>
  <files>src/registry-explorer/ui/shell.ts, public/styles/registry-explorer.css</files>
  <read_first>
    @src/registry-explorer/ui/shell.ts
    @src/registry-explorer/ui/componentView.ts
    @src/registry-explorer/ui/discoveryView.ts
    @public/styles/registry-explorer.css
    @package.json
    @.planning/phases/03-component-first-discovery/03-CONTEXT.md
  </read_first>
  <action>Add shell event delegation for `[data-discover-component]` so selecting it sets `searchTerm` to the exact component label/key, updates the search input value, clears selected profile/candidate state, and routes to `currentView: 'discover'` per D-21 and D-22. Keep focus and matrix navigation unchanged except for status-aware rendering. Run the full project gate `pnpm verify` because this final plan touches source, tests, generated-data validation expectations, and build output.</action>
  <verify>
    <automated>pnpm verify</automated>
    <automated>! rg -n "approved|certified|trusted|secure badge|audited by Registry Atlas|Copy install|Copy view|Add to queue|Batch install|share URL|npx shadcn" src index.html public/styles/registry-explorer.css</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `shell.ts` contains `[data-discover-component]`, assigns `roots.searchInput.value`, and sets `currentView: 'discover'`.
    - CLI assertion: `pnpm verify` exits with code 0.
    - CLI assertion: the `rg` command finds none of the prohibited trust or Phase 4 action strings.
  </acceptance_criteria>
  <done>Users can move from secondary component facets back to primary discovery, and the full app passes the established verification gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Derived status data -> secondary UI | Coverage/status fields influence browsing conclusions. |
| Component facet click -> search state | UI-generated text becomes search input and result filtering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Spoofing | focus/component/matrix status UI | mitigate | Render explicit status labels and avoid binary cells that imply verified coverage. |
| T-03-05-02 | Tampering | component facet search wiring | mitigate | Use controlled component keys/labels from app data and route through existing search state. |
| T-03-05-03 | Elevation of Privilege | secondary external links | mitigate | Continue `renderExternalLink` and no install execution/copy controls. |
| T-03-05-04 | Denial of Service | matrix rendering | accept | Static local matrix is bounded by registry count and configured matrix columns. |
| T-03-05-SC | Tampering | npm installs | accept | No new package-manager installs are planned for this phase plan. |
</threat_model>

<verification>
Run `pnpm test -- --run tests/registry-explorer/grouping.test.ts`, `pnpm build`, then final `pnpm verify`.
</verification>

<success_criteria>
This plan succeeds when focus, component, and matrix remain accessible secondary modes, each displays coverage/status honesty, component facets route to discovery, and full verification passes.
</success_criteria>

<output>
Create `.planning/phases/03-component-first-discovery/03-05-SUMMARY.md` when done.
</output>
